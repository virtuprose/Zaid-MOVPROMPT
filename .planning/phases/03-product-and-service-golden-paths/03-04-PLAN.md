---
phase: 03-product-and-service-golden-paths
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - packages/contracts/src/assets.ts
  - packages/contracts/src/creator.ts
  - packages/contracts/src/contracts.test.ts
  - packages/db/src/schema.ts
  - packages/db/migrations/0018_add_footage_asset_kind.sql
  - apps/api/src/campaign-eligibility.ts
  - apps/api/src/guest-claim-service.ts
  - apps/api/src/guest-claim-service.postgres.test.ts
  - apps/api/src/runtime-services.ts
  - apps/api/src/generation-routes.ts
  - apps/api/src/generation-repository.ts
  - apps/api/src/generation-service.ts
  - apps/api/src/generation-service.test.ts
  - apps/api/src/generation.test.ts
autonomous: true
requirements: [SOURCE-03, CREATE-05, CREATE-07, CREATE-09, CREATE-10]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-03-SUMMARY.md
  - packages/contracts/src/assets.ts
  - packages/contracts/src/guest-claims.ts
  - apps/api/src/guest-claim-service.ts
  - apps/api/src/generation-service.ts
artifacts:
  - path: packages/contracts/src/creator.ts
    provides: Strict beginner campaign presenter configuration and rights contract
  - path: apps/api/src/campaign-eligibility.ts
    provides: Shared server assertion used by claim, quote and submission
  - path: apps/api/src/generation.test.ts
    provides: Negative HTTP tests for unsupported/unauthorized presenter requests
acceptance_criteria:
  - No presenter is valid by default; Digital Twin is outside the beginner campaign contract.
  - AI UGC is accepted only when both published template policy and server capability support the selected language.
  - Uploaded spokesperson is accepted only with the exact owner/project footage asset, verified checksum/media metadata and explicit rights.
  - Claim, authenticated quote and render submission revalidate presenter eligibility server-side; browser controls are never authorization.
must_haves:
  truths:
    - Tampering with browser presenter fields cannot create an unsupported or unauthorized render.
    - Guest footage becomes a private owner-scoped asset before the claimed project can be quoted or submitted.
    - Negative API responses disclose no other-user asset, template recipe or provider detail.
  artifacts:
    - path: packages/db/migrations/0018_add_footage_asset_kind.sql
      provides: Portable PostgreSQL footage asset kind
    - path: apps/api/src/guest-claim-service.postgres.test.ts
      provides: Guest claim manifest/rights rejection proof
    - path: apps/api/src/generation-service.test.ts
      provides: Quote/start presenter revalidation matrix
  key_links:
    - from: apps/api/src/guest-claim-service.ts
      to: apps/api/src/campaign-eligibility.ts
      via: Untrusted snapshot is validated before repository start/finalize
      pattern: assertCampaignPresenterEligibility
    - from: apps/api/src/generation-service.ts
      to: apps/api/src/generation-repository.ts
      via: Exact owner/project footage and template policy are reloaded before quote/start
      pattern: findOwnedPresenterAsset|findPublishedTemplateVersion
---

<objective>
Enforce D-18 through D-21 presenter eligibility at shared contract, guest claim, quote and final submission boundaries.

Purpose: Beginner UI can guide users, but only the server can authorize AI UGC or a specific owned spokesperson recording.
Output: Footage asset support, strict presenter contract, reusable API assertion and negative boundary tests.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-RESEARCH.md

<interfaces>
The normalized campaign configuration carries one beginner presenter object: none, ai_ugc, or uploaded_spokesperson. Uploaded spokesperson names one opaque asset UUID plus rights attestation. Guest claim manifest retains that UUID as the canonical private asset identity. Existing withUserTransaction and owner/project object-key rules remain authoritative.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-04-01: Carry one rights-attested footage asset through shared contracts</name>
  <reversibility rating="costly">Adding a PostgreSQL enum value and serialized presenter contract affects portable migrations and persisted project configuration.</reversibility>
  <files>packages/contracts/src/assets.ts, packages/contracts/src/creator.ts, packages/contracts/src/contracts.test.ts, packages/db/src/schema.ts, packages/db/migrations/0018_add_footage_asset_kind.sql</files>
  <behavior>
    - A footage upload has bounded video MIME, size, checksum, duration and opaque asset UUID metadata per SOURCE-03.
    - Uploaded spokesperson presenter data requires that asset UUID and a versioned explicit person/media rights attestation.
    - Beginner presenter parsing excludes Digital Twin and rejects unknown fields/provider identifiers.
  </behavior>
  <action>Write contract failures first. Add the footage asset kind through an additive repository-owned migration and Drizzle schema. Define a strict CampaignPresenterSchema used by campaign configuration with none, ai_ugc and uploaded_spokesperson only; make asset/rights fields discriminated and bounded. Keep provider references and signed/object URLs outside public contracts.</action>
  <verify><automated>bun run --cwd packages/contracts test -- contracts &amp;&amp; bun run --cwd packages/contracts typecheck &amp;&amp; bun run --cwd packages/db check</automated></verify>
  <done>One consented footage asset parses as uploaded spokesperson input while missing rights, non-video media, Digital Twin and provider fields fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-02: Reject ineligible presenter data during guest claim</name>
  <files>apps/api/src/campaign-eligibility.ts, apps/api/src/guest-claim-service.ts, apps/api/src/guest-claim-service.postgres.test.ts, apps/api/src/runtime-services.ts, apps/api/src/generation-routes.ts</files>
  <behavior>
    - None is accepted without media; AI UGC is rejected at claim when template policy/language/capability evidence is absent.
    - Uploaded spokesperson requires a matching footage entry in the exact snapshot manifest plus matching rights asset UUID.
    - Replay of the identical valid snapshot returns the same operation; changed presenter/media/rights under the same pending ID conflicts.
  </behavior>
  <action>Build the negative PostgreSQL-backed cases before the assertion. Create a CampaignEligibilityService whose injected server dependencies reload published template policy and resolve current capability/language support. Inject it from runtime-services into GuestClaimService; parse normalized configuration inside claimGuestDraft/startClaim and invoke it before repository mutation. Match uploaded spokesperson only by manifest UUID/kind, not filename/MIME supplied elsewhere. Add the stable presenter_configuration_ineligible API mapping. Preserve Phase 2 digest/idempotency behavior and fail before project visibility when presenter validation fails.</action>
  <verify><automated>bun run --cwd apps/api test -- guest-claim-service.postgres.test.ts -t "presenter eligibility" &amp;&amp; bun run --cwd apps/api typecheck</automated></verify>
  <done>Guest claim accepts one valid owned-footage snapshot and rejects unsupported AI UGC, wrong kind/UUID, missing rights and replay mutation without creating a ready project.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-03: Revalidate presenter ownership at quote and submission</name>
  <files>apps/api/src/campaign-eligibility.ts, apps/api/src/generation-repository.ts, apps/api/src/generation-service.ts, apps/api/src/generation-service.test.ts, apps/api/src/generation.test.ts</files>
  <behavior>
    - Owned quote and startRender reload template presenter policy, language support and capability availability from server sources.
    - Uploaded spokesperson reloads the exact owner/project footage asset and requires checksum, positive size/duration and video MIME.
    - Another user's asset, missing rights, unsupported language/template, disabled AI UGC and Digital Twin all fail before reservation/provider work.
  </behavior>
  <action>Add service and HTTP negative tests first. Extend GenerationRepository with an owner/project/asset-ID footage lookup under withUserTransaction. Invoke the same campaign assertion during authenticated quote and startRender after loading the immutable version/template and before pricing/reservation. Map one stable presenter_configuration_ineligible error without leaking whether an asset belongs to another user. Assert the generation domain start method is never called on denial.</action>
  <verify><automated>bun run --cwd apps/api test -- generation-service.test.ts generation.test.ts -t "presenter eligibility" &amp;&amp; bun run --cwd apps/api typecheck</automated></verify>
  <done>All three server boundaries enforce identical presenter rules and every negative API case proves zero charge, reservation and provider submission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Guest campaign to claim API | Presenter mode, asset UUID and rights are untrusted browser data. |
| Project version to generation API | Persisted JSON may be stale or malicious and must be revalidated against current owner/template/capability state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-06 | Spoofing / Elevation of privilege | Spokesperson asset | high | mitigate | Exact UUID, owner/project DB predicate, verified footage metadata and rights binding at claim/quote/start. |
| T-03-11 | Tampering | Presenter mode/template/language | high | mitigate | Strict shared schema plus server reload of template and capability state at every boundary. |
| T-03-12 | Information disclosure | Cross-user presenter denial | high | mitigate | Uniform not-eligible error and negative HTTP tests with no asset-owner detail. |
</threat_model>

<verification>
- Contract, migration, PostgreSQL claim, service and HTTP negative tests pass.
- Browser presenter state is not consulted as authorization.
- Denied cases produce no project-ready state, quote, run, charge or provider call.
</verification>

<success_criteria>
- Unsupported AI UGC and unauthorized human footage are impossible through tested public boundaries.
- Valid owner footage survives claim and is accepted consistently by quote/start.
- No high-severity presenter threat remains unresolved.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-04-SUMMARY.md` when done.
</output>
