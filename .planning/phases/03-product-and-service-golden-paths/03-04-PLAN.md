---
phase: 03-product-and-service-golden-paths
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - apps/web/src/features/create/campaignSetupRules.ts
  - apps/web/src/features/create/PresenterChoice.tsx
  - apps/web/src/features/create/CampaignSetupStep.tsx
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/PresenterStep.test.tsx
  - apps/web/src/features/create/CampaignSetupStep.test.tsx
  - apps/web/src/features/create/creator.css
  - apps/web/src/i18n/translations/en.ts
  - apps/web/src/i18n/translations/ar.ts
autonomous: true
requirements: [CREATE-07, CREATE-08, CREATE-10]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-03-SUMMARY.md
  - .planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
  - apps/web/src/features/create/types.ts
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/projectStore.ts
artifacts:
  - path: apps/web/src/features/create/campaignSetupRules.ts
    provides: Outcome-aware defaults, required fields, and Kuwait/KWD validation
  - path: apps/web/src/features/create/PresenterChoice.tsx
    provides: Fail-closed presenter choices and rights state
  - path: apps/web/src/features/create/CampaignSetupStep.tsx
    provides: Progressive beginner campaign settings
acceptance_criteria:
  - No presenter is selected by default and unapproved presenter modes do not render.
  - Uploaded spokesperson is blocked until source footage and explicit rights are present.
  - Kuwait is fixed; KWD uses three decimals; interface and campaign language remain independent.
  - Outcome-aware CTA, price/offer, WhatsApp/booking, tone, subtitles, audio, resolution, and ratio persist unchanged.
must_haves:
  truths:
    - A beginner sees only presenter choices that are actually usable for the selected template and language.
    - Campaign setup reveals only relevant fields while preserving every previously entered value.
    - Defaults maximize first completion without claiming they came from the imported business source.
  artifacts:
    - path: apps/web/src/features/create/campaignSetupRules.ts
      provides: Pure presenter/settings eligibility and validation
    - path: apps/web/src/features/create/PresenterChoice.tsx
      provides: Accessible presenter selection and person-rights requirement
    - path: apps/web/src/features/create/CampaignSetupStep.test.tsx
      provides: Kuwait, language, CTA, offer, delivery, and persistence proof
  key_links:
    - from: apps/web/src/features/create/PresenterChoice.tsx
      to: apps/web/src/features/create/types.ts
      via: catalog-backed template presenter compatibility
      pattern: presenterCompatibility
    - from: apps/web/src/features/create/CampaignSetupStep.tsx
      to: apps/web/src/features/create/contracts.ts
      via: controlled CampaignSettings update callback
      pattern: CampaignSettings
---

<objective>
Implement D-18 through D-22 as one simple presenter and campaign setup step that is capability-aware, Kuwait-correct, and lossless.

Purpose: Users must configure a usable campaign without technical controls or unexpected fields, while human media remains rights-safe.
Output: Pure setup rules, presenter eligibility, progressive campaign fields, bilingual copy, and persistence tests.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
@.planning/phases/03-product-and-service-golden-paths/03-RESEARCH.md
@.planning/phases/03-product-and-service-golden-paths/03-03-SUMMARY.md

<interfaces>
CampaignSetupStep is controlled by CreateStudio and updates the existing CampaignSettings/CreatorProject fields. campaignSetupRules receives normalized source, outcome, template facts, campaign language, assets, presenter selection, and rights; it returns visible fields, defaults, validation errors, and eligibility without provider identifiers.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-04-01: Complete the no-presenter Kuwait setup path</name>
  <files>apps/web/src/features/create/campaignSetupRules.ts, apps/web/src/features/create/PresenterChoice.tsx, apps/web/src/features/create/CampaignSetupStep.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/CampaignSetupStep.test.tsx</files>
  <behavior>
    - No presenter is selected by default per D-18/D-22.
    - Kuwait is displayed as a fixed market note; KWD formats to three decimal places; interface language does not overwrite campaign language per D-21.
    - The baseline setup includes language, outcome-specific CTA destination, tone, subtitles, supported audio, 720p, and 9:16, with values persisted into the same draft per D-20/D-22.
  </behavior>
  <action>Write the failing no-presenter tracer first. Implement pure setup defaults/validation and compose PresenterChoice plus CampaignSetupStep using native groups and existing controls. Wire their narrow callbacks to CreateStudio without creating a second draft state. Keep defaults explicitly identified as defaults rather than imported facts, use the selected template capabilities for audio, and format KWD with three decimals. Maintain D-03 one primary Review campaign action and preserve Back without clearing setup.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/CampaignSetupStep.test.tsx -t "no presenter Kuwait setup" &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>A user can complete a truthful no-presenter Kuwait campaign setup and reload the same language, CTA, delivery, audio, subtitle, resolution, and ratio values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-02: Enforce catalog-backed presenter rights</name>
  <files>apps/web/src/features/create/campaignSetupRules.ts, apps/web/src/features/create/PresenterChoice.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/PresenterStep.test.tsx</files>
  <behavior>
    - AI UGC appears only when the selected template plus server/catalog language availability explicitly permits it; otherwise no inert option is shown per D-19.
    - Uploaded spokesperson requires a real-footage source asset and explicit person-rights confirmation before continuing per D-11/D-19.
  </behavior>
  <action>Commit failing presenter cases, then make presenter availability data-driven from Plan 03-03 catalog facts and filter every unapproved presenter value. Add footage and rights validation without converting footage into a presenter automatically. A capability change invalidates only the incompatible presenter choice and explains the recovery; it does not erase source or campaign facts. Keep no presenter as the deterministic fallback per D-18/D-19.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/PresenterStep.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Only usable presenters render, no presenter remains the default, and uploaded spokesperson selection fails closed until source footage and rights are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-03: Complete progressive bilingual campaign settings</name>
  <files>apps/web/src/features/create/CampaignSetupStep.tsx, apps/web/src/features/create/CampaignSetupStep.test.tsx, apps/web/src/features/create/creator.css, apps/web/src/i18n/translations/en.ts, apps/web/src/i18n/translations/ar.ts</files>
  <behavior>
    - Price/offer appears only for relevant outcomes, WhatsApp for orders, and booking link/location for bookings; switching outcomes retains compatible values per D-20.
    - Every allowed setup field has complete English/Arabic label, help, inline error, and direction-safe value rendering per D-21/D-29.
    - Setup loading, invalid, capability-changed, and offline states preserve all compatible values and expose one primary recovery per D-25/D-26.
  </behavior>
  <action>Write the progressive-field, Arabic, and recovery tests first. Complete the outcome-aware fields and full English/Arabic copy, then apply the existing amber selection/focus, semantic status, logical CSS, 44px targets, field associations, polite status, and reduced-motion rules per D-27 through D-29. Values remain controlled and saved on each transition; defaults are never labelled as imported facts.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/CampaignSetupStep.test.tsx src/features/create/PresenterStep.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Every outcome reveals the correct accessible fields, all settings round-trip, and English/Arabic recovery states preserve the complete setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog availability to presenter UI | Server/catalog approval controls whether an AI cast option can be offered. |
| User footage and rights to generation configuration | Person media becomes eligible for presenter use only after explicit attestation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Spoofing / Information disclosure | Uploaded spokesperson | high | mitigate | Require owned source footage plus explicit person-rights state; filter unapproved modes and test every bypass path. |
| T-03-07 | Tampering | Campaign settings | high | mitigate | Controlled fields, Zod-backed draft, outcome-aware validation, direction-safe formatting, and exact round-trip tests. |
| T-03-08 | Denial of service | Capability changes | medium | mitigate | Fail closed on the affected choice, retain the complete draft, and expose one recovery action. |
</threat_model>

<verification>
- Presenter and campaign setup focused suites pass in English and Arabic.
- Tests prove no-presenter default, catalog-gated AI option, footage-plus-rights requirement, Kuwait/KWD formatting, and every outcome-specific field.
- Template Mode renders no provider/model/prompt/timeline terms.
</verification>

<success_criteria>
- CREATE-07 and CREATE-08 are fully usable and persisted.
- CREATE-10 state and accessibility contracts cover presenter/campaign setup.
- No unresolved high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-04-SUMMARY.md` when done.
</output>
