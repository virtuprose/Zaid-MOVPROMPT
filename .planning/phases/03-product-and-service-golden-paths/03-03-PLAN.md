---
phase: 03-product-and-service-golden-paths
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/contracts/src/creator.ts
  - packages/contracts/src/contracts.test.ts
  - apps/api/src/generation-repository.ts
  - apps/api/src/generation-service.ts
  - apps/api/src/generation-service.test.ts
  - apps/api/src/generation-routes.ts
  - apps/web/src/features/create/templateQuoteState.ts
  - apps/web/src/features/create/templateQuoteState.test.ts
  - apps/web/src/features/create/useTemplateQuotes.ts
  - apps/web/src/features/create/CreateStudio.tsx
autonomous: true
requirements: [CREATE-04, CREATE-05, CREATE-10]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-02-SUMMARY.md
  - .planning/phases/03-product-and-service-golden-paths/03-RESEARCH.md
  - packages/contracts/src/creator.ts
  - apps/api/src/generation-service.ts
  - apps/web/src/lib/api/portableApiClient.ts
artifacts:
  - path: apps/api/src/generation-service.ts
    provides: Server-authoritative template eligibility and configuration-bound quote validation
  - path: apps/web/src/features/create/templateQuoteState.ts
    provides: Quote lifecycle keyed by template version and exact configuration hash
  - path: apps/web/src/features/create/useTemplateQuotes.ts
    provides: Existing internal quote API coordinator for selectable recommendations
acceptance_criteria:
  - Catalog data determines whether a template/configuration is eligible, never its price.
  - Every selectable template obtains an existing server generation quote bound to templateVersionId and exact configuration.
  - Loading, unavailable, expired and changed quote states are explicit and cannot enable selection or Generate without a fresh quote.
  - No client fallback credit amount or locally invented quote appears in state or UI inputs.
must_haves:
  truths:
    - An eligible template is not treated as ready-to-generate until its configuration-bound server quote succeeds.
    - Changing any quote-affecting fact, template, language, presenter, ratio, resolution, audio or reference invalidates the prior quote.
    - Quote failure preserves the complete draft and offers an explicit retry without inventing a price.
  artifacts:
    - path: apps/api/src/generation-service.test.ts
      provides: Positive and negative catalog/quote binding proof
    - path: apps/web/src/features/create/templateQuoteState.test.ts
      provides: Loading, unavailable, expired, changed and retry state proof
  key_links:
    - from: apps/web/src/features/create/useTemplateQuotes.ts
      to: apps/web/src/lib/api/portableApiClient.ts
      via: Existing generationQuote API with templateVersionId and exact configuration
      pattern: generationQuote
    - from: apps/api/src/generation-service.ts
      to: apps/api/src/generation-repository.ts
      via: Published catalog eligibility is checked before authoritative pricing
      pattern: findPublishedTemplateVersion
---

<objective>
Bind D-15 template eligibility to the existing authoritative generation quote boundary without allowing catalog metadata to become pricing authority.

Purpose: A recommendation can be relevant and still be unavailable to generate; the server must own price, expiry and configuration binding.
Output: Server eligibility checks, quote-lifecycle state, internal quote coordination, and focused negative tests.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-RESEARCH.md
@.planning/phases/03-product-and-service-golden-paths/03-PATTERNS.md

<interfaces>
Reuse CreateGenerationQuoteRequestSchema, GenerationQuoteSchema, generationQuote(), published template version lookup, hashGenerationConfiguration(), and CreateStudio draft updates. Provider/model identifiers remain server-only. Phase 3 uses existing internal MovPrompt APIs and creates no external API coverage artifact.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-03-01: Quote one eligible template from its exact configuration</name>
  <files>packages/contracts/src/creator.ts, packages/contracts/src/contracts.test.ts, apps/api/src/generation-repository.ts, apps/api/src/generation-service.ts, apps/api/src/generation-service.test.ts</files>
  <behavior>
    - One published Kuwait template whose declared goal, language, ratio, required inputs and capability match receives an estimate quote bound to that template version and exact configuration per D-15.
    - Catalog eligibility fields are parsed from server-owned template data; credits, pricing version, expiry and configuration hash come only from GenerationPricing and GenerationService.
    - A quote request that omits or contradicts required catalog inputs is rejected before pricing and returns no fallback credits.
  </behavior>
  <action>Write the failing service cases first. Extend the published template contract/repository projection with bounded eligibility metadata already present in the immutable catalog recipe and columns. Add a server eligibility assertion before pricing in createQuote, then keep the existing boundConfiguration and hashGenerationConfiguration flow authoritative. Do not add price fields to PublicTemplate or derive price from duration/catalog metadata.</action>
  <verify><automated>bun run --cwd apps/api test -- generation-service.test.ts -t "template quote eligibility" &amp;&amp; bun run --cwd packages/contracts test -- contracts &amp;&amp; bun run --cwd apps/api typecheck</automated></verify>
  <done>One catalog-eligible template receives a real configuration-bound quote and one ineligible request fails without returning credits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-02: Reject stale and mismatched template quotes</name>
  <files>apps/api/src/generation-service.ts, apps/api/src/generation-service.test.ts, apps/api/src/generation-repository.ts, apps/api/src/generation-routes.ts</files>
  <behavior>
    - Unsupported goal, language, ratio, capability or missing required input is rejected for guest estimate and owned project quote.
    - Expired quote, changed price, changed template version and changed configuration are rejected at submission with stable error codes.
    - StartRender never trusts a prior browser eligibility decision and revalidates the current published template contract.
  </behavior>
  <action>Add the negative matrix before implementation. Reuse quote_expired, quote_configuration_mismatch and quote_price_changed, and add one stable template_configuration_ineligible application error mapped by generation routes. Revalidate template eligibility during owned quoting and startRender before any reservation/provider work. Sanitize responses so catalog internals, provider IDs and owned configuration are not disclosed.</action>
  <verify><automated>bun run --cwd apps/api test -- generation-service.test.ts -t "template quote rejection" &amp;&amp; bun run --cwd apps/api typecheck</automated></verify>
  <done>Every stale, changed or catalog-ineligible quote path fails closed before any charge, reservation or provider submission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-03: Coordinate per-template quote lifecycle in the creator</name>
  <files>apps/web/src/features/create/templateQuoteState.ts, apps/web/src/features/create/templateQuoteState.test.ts, apps/web/src/features/create/useTemplateQuotes.ts, apps/web/src/features/create/CreateStudio.tsx</files>
  <behavior>
    - Quote state is keyed by templateVersionId plus the exact normalized configuration hash and cannot be reused after a quote-affecting edit.
    - State distinguishes idle, loading, ready, unavailable, expired and changed, retaining request ID and retryability without provider details.
    - Only ready and unexpired server responses expose credits/entitlement; every other state exposes no price.
  </behavior>
  <action>Start with the pure state-machine tests. Implement a request coordinator around portableCreatorApi.generationQuote with cancellation/last-request-wins protection and explicit retry. Have CreateStudio request quotes only for catalog-eligible recommendation candidates and invalidate them on every relevant draft change per D-26/D-29. Do not show selection UI here; Plan 03-05 consumes this state.</action>
  <verify><automated>bun run --cwd apps/web test -- src/features/create/templateQuoteState.test.ts &amp;&amp; bun run --cwd apps/web typecheck</automated></verify>
  <done>Every eligible candidate has an honest server quote lifecycle that cannot survive a configuration change or fall back to a client price.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Catalog/configuration to quote API | Browser catalog state and configuration are untrusted; the server reloads published template data. |
| Quote response to creator state | Expiring configuration-bound price must not be reused after edits or races. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04 | Tampering | Template eligibility and configuration | high | mitigate | Server reloads published immutable template and validates exact configuration at quote and submission. |
| T-03-05 | Tampering / Repudiation | Quote lifecycle | high | mitigate | Configuration hash, expiry, pricing version, last-request-wins state and negative tests. |
| T-03-10 | Information disclosure | Quote errors | medium | mitigate | Stable codes/request ID only; no provider, recipe or owned configuration disclosure. |
</threat_model>

<verification>
- Focused contract/API/web quote tests pass.
- No PublicTemplate or browser helper contains a credit fallback.
- Guest estimate and authenticated quote use the same catalog eligibility rules.
</verification>

<success_criteria>
- Catalog decides eligibility; the existing server pricing stack decides price.
- Expired, changed or unavailable quotes cannot enable selection/generation.
- No high-severity quote threat remains unresolved.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-03-SUMMARY.md` when done.
</output>
