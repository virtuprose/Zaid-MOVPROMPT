---
phase: 03-product-and-service-golden-paths
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/web/src/features/create/OutcomeStep.tsx
  - apps/web/src/features/create/templateRecommendations.ts
  - apps/web/src/features/create/TemplateRecommendations.tsx
  - apps/web/src/features/create/templateRecommendations.test.ts
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/types.ts
  - apps/web/src/features/create/templateCatalogMapper.ts
  - packages/contracts/src/creator.ts
  - packages/creative-engine/src/catalog.ts
  - apps/web/src/features/create/TemplateGrid.tsx
  - apps/web/src/features/create/templateMedia.ts
  - apps/web/src/features/create/TemplateRecommendation.test.tsx
  - apps/web/src/features/create/creator.css
autonomous: true
requirements: [CREATE-02, CREATE-03, CREATE-04, CREATE-05, CREATE-06, CREATE-10]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-02-SUMMARY.md
  - .planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
  - apps/web/src/features/create/TemplateGrid.tsx
  - apps/web/src/features/create/templateMedia.ts
  - packages/creative-engine/src/catalog.ts
artifacts:
  - path: apps/web/src/features/create/templateRecommendations.ts
    provides: Deterministic recommendation scoring and explanations
  - path: apps/web/src/features/create/TemplateRecommendations.tsx
    provides: Ranked truthful recommendation surface
  - path: apps/web/src/features/create/TemplateRecommendation.test.tsx
    provides: Template metadata and preview-truth proof
acceptance_criteria:
  - Every locked outcome produces a deterministic ranked set or one honest recovery state.
  - Source-first shows at most three recommendations and template-first preserves its immutable selection.
  - Selectable cards disclose result, inputs, duration, formats, presenter compatibility, preview truth, and cost state.
  - Only verified template-specific motion exposes Play; static directions remain non-playable.
must_haves:
  truths:
    - A beginner can choose a business outcome and understand why each recommendation fits.
    - A template cannot be selected when its required input, presenter, language, format, or availability contract is false.
    - Static art never looks or behaves like a completed video preview.
  artifacts:
    - path: apps/web/src/features/create/templateRecommendations.ts
      provides: Pure max-three scorer with stable tie-break and explicit missing inputs
      exports: [recommendTemplates]
    - path: apps/web/src/features/create/TemplateRecommendations.tsx
      provides: Outcome recommendations and empty recovery state
    - path: apps/web/src/features/create/TemplateGrid.tsx
      provides: Verified-motion/static-direction affordance boundary
  key_links:
    - from: apps/web/src/features/create/OutcomeStep.tsx
      to: apps/web/src/features/create/templateRecommendations.ts
      via: Selected outcome plus normalized facts and assets
      pattern: recommendTemplates
    - from: apps/web/src/features/create/TemplateRecommendations.tsx
      to: apps/web/src/features/create/templateMedia.ts
      via: previewVideo is the sole Play gate
      pattern: previewVideo
---

<objective>
Add D-12 through D-17 outcome-first recommendations and truthful template selection, with source-first and template-first converging on the same draft.

Purpose: Beginners need a small, explainable choice set, while every card must tell the truth about inputs, availability, media, and price.
Output: Outcome step, deterministic recommendation engine, capability-aware cards, honest empty states, and verified preview behavior.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
@.planning/phases/03-product-and-service-golden-paths/03-PATTERNS.md
@.planning/phases/03-product-and-service-golden-paths/03-02-SUMMARY.md

<interfaces>
recommendTemplates receives normalized source/facts/assets, outcome, campaign language, ratio, presenter need, and public template facts. It returns ordered recommendations with score, reasons, selectable, missingInputs, and disabledReason. Stable tie-break is catalog order then template ID.

PublicTemplate and CreatorTemplate are server/catalog facts. Browser labels cannot infer presenter approval, availability, or verified media from the template name or poster.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-03-01: Produce explainable recommendations from one confirmed outcome</name>
  <files>apps/web/src/features/create/OutcomeStep.tsx, apps/web/src/features/create/templateRecommendations.ts, apps/web/src/features/create/TemplateRecommendations.tsx, apps/web/src/features/create/templateRecommendations.test.ts, apps/web/src/features/create/CreateStudio.tsx</files>
  <behavior>
    - The nine D-12 outcomes are visible as plain-language radio options and persist unchanged.
    - Identical product facts, outcome, assets, language, duration, ratio, and presenter need return the same ordered maximum of three recommendations and the same reasons.
    - No selectable match returns one explicit missing-input/recovery state and never silently changes outcome or template.
  </behavior>
  <action>Write the failing product-outcome tracer first. Implement the pure recommendation scorer using only visible normalized inputs and published template facts, with stable tie-breaks and concise reason codes converted to beginner copy. Compose OutcomeStep and TemplateRecommendations as controlled components and wire them into CreateStudio after confirmed facts. Preserve D-03 single-next-action hierarchy, D-13 maximum-three recommendations, and D-14 entry convergence; Browse all remains secondary and retains source/fact/outcome state.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/templateRecommendations.test.ts &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>All outcomes produce deterministic, explainable recommendations or one honest recovery route, and the chosen recommendation cannot rewrite confirmed source intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-02: Make template eligibility an explicit catalog contract</name>
  <files>packages/contracts/src/creator.ts, packages/creative-engine/src/catalog.ts, apps/web/src/features/create/types.ts, apps/web/src/features/create/templateCatalogMapper.ts, apps/web/src/features/create/templateRecommendations.test.ts</files>
  <behavior>
    - Selectable template metadata includes expected result, required inputs, duration, supported formats, presenter compatibility, preview type, and authoritative cost state per D-15.
    - Development/review or unsupported combinations are labelled and non-selectable with one exact reason per D-17.
  </behavior>
  <action>Begin with failing eligibility cases in the existing recommendation test suite. Extend PublicTemplate/CreatorTemplate and the catalog mapper with explicit compatibility, selectability, required input, expected result, duration, formats, presenter compatibility, preview type, and cost state sourced from contracts/catalog rather than JSX inference. Unsupported combinations carry a typed disabled reason per D-17 so the following rendering task cannot reinterpret eligibility.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/templateRecommendations.test.ts &amp;&amp; bun run --cwd packages/creative-engine test -- catalog &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Every recommendation is truthful and selectable only when its complete catalog contract is satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-03: Preserve verified-motion and static-direction truth</name>
  <files>apps/web/src/features/create/TemplateRecommendations.tsx, apps/web/src/features/create/TemplateGrid.tsx, apps/web/src/features/create/templateMedia.ts, apps/web/src/features/create/TemplateRecommendation.test.tsx, apps/web/src/features/create/creator.css</files>
  <behavior>
    - A Play control exists only when verified template-specific motion is present; static direction art exposes View direction and no player-shaped chrome per D-16.
    - Zero, one, two, and three recommendation layouts follow the UI-SPEC without horizontal overflow, and selected/disabled states are textual as well as visual.
    - The existing preview dialog retains focus trap, labelled close, focus return, loading, and unavailable-media behavior.
  </behavior>
  <action>Write the failing disclosure, media-truth, and zero/one/many cases first. Make TemplateRecommendations render every D-15 disclosure, quote loading/unavailable state, selected text, and typed disabled reason. Update TemplateGrid and templateMedia so previewVideo remains the sole Play gate and static directions never receive duration progress or generated-example wording. Preserve the existing dialog primitive unchanged unless a test demonstrates a regression. Apply the existing token palette, 44px targets, visible focus, logical layout, and reduced-motion behavior to card states.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/TemplateRecommendation.test.tsx src/features/create/TemplateGrid.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Verified motion is playable through the existing accessible dialog, while every static direction remains visibly and technically non-playable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog/API facts to browser recommendation | Server/catalog availability and media truth become user-visible selection guidance. |
| Browser outcome to template selection | Untrusted UI state influences the configuration later quoted and claimed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | Recommendation ranking | medium | mitigate | Pure deterministic scorer, stable tie-break, catalog-backed eligibility, and exhaustive outcome tests. |
| T-03-05 | Spoofing / Repudiation | Template media and cost state | high | mitigate | Explicit preview/selectability fields, verified-motion allowlist, non-playable static art, and fail-closed quote state. |
| T-03-03 | Tampering | Template-first/source-first convergence | high | mitigate | Immutable selected template ID plus canonical source facts; selection handlers may not rewrite confirmed intent. |
</threat_model>

<verification>
- All nine outcomes, deterministic rank order, max-three behavior, missing-input recovery, and template-first preservation pass tests.
- Template cards disclose every required fact and only verified video gets Play.
- No model/provider/prompt/timeline terminology appears in outcome or recommendation copy.
</verification>

<success_criteria>
- CREATE-03 through CREATE-06 are proven by focused unit/component tests.
- CREATE-02 convergence remains intact after template selection.
- CREATE-10 empty/loading/unavailable states each have one primary recovery action.
- No unresolved high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-03-SUMMARY.md` when done.
</output>
