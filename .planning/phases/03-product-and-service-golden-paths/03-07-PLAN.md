---
phase: 03-product-and-service-golden-paths
plan: "07"
type: execute
wave: 7
depends_on: ["03-06"]
files_modified:
  - apps/web/src/features/create/CampaignReviewStep.tsx
  - apps/web/src/features/create/CampaignReviewStep.test.tsx
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/GoldenPathStates.test.tsx
  - apps/web/src/features/create/AuthGateDialog.test.tsx
  - apps/web/src/features/create/creator.css
  - apps/web/src/i18n/translations/en.ts
  - apps/web/src/i18n/translations/ar.ts
autonomous: true
requirements: [SOURCE-02, SOURCE-03, CREATE-02, CREATE-05, CREATE-07, CREATE-08, CREATE-09, CREATE-10]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-06-SUMMARY.md
  - .planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/AuthGateDialog.tsx
  - apps/web/src/features/create/guestClaimRecovery.ts
artifacts:
  - path: apps/web/src/features/create/CampaignReviewStep.tsx
    provides: Exact grouped product/service review and edit routing
  - path: apps/web/src/features/create/CampaignReviewStep.test.tsx
    provides: Review completeness and quote/rights gating proof
  - path: apps/web/src/features/create/GoldenPathStates.test.tsx
    provides: Quote/auth recovery state retention proof
acceptance_criteria:
  - Review derives directly from the normalized draft and displays all generation-used facts, media, provenance, presenter, rights and delivery choices.
  - Generate is enabled only with a ready/unexpired authoritative quote whose template/configuration binding still matches.
  - Quote loading/unavailable/expired/changed, offline, upload, worker, capability and cancelled-auth states preserve the exact draft.
  - Generate-time email/configured social auth returns to the same review and pending intent once.
must_haves:
  truths:
    - The user can verify the exact campaign in a calm summary before authentication.
    - Review, claimed project version, quote configuration and pending submission derive from one canonical draft.
    - Every failure has one honest next action and never discards source/media/settings.
  artifacts:
    - path: apps/web/src/features/create/CampaignReviewStep.tsx
      provides: Six semantic review groups and exact edit actions
    - path: apps/web/src/features/create/GoldenPathStates.test.tsx
      provides: Full review/quote/auth recovery matrix
  key_links:
    - from: apps/web/src/features/create/CampaignReviewStep.tsx
      to: apps/web/src/features/create/contracts.ts
      via: Rows derive from CreationDraft CampaignSource/facts/presenter/campaign
      pattern: source.*facts|presenter|campaign
    - from: apps/web/src/features/create/CreateStudio.tsx
      to: apps/web/src/features/create/useTemplateQuotes.ts
      via: Generate consumes the selected current server quote only
      pattern: quoteId|configurationHash|expiresAt
---

<objective>
Complete D-23 through D-26 with an exact final review and one honest Generate-time authentication handoff.

Purpose: The user must know exactly what is ready, what is blocked and that every recovery preserves the campaign.
Output: Product/service review, edit routing, authoritative quote gate, auth handoff and complete interaction-state tests.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md

<interfaces>
Reuse Phase 2 pendingGenerationId, IndexedDB snapshot, safe next/callback, claim/replay and auth dialog contracts. Consume the selected immutable template plus current quote from Plans 03-03/03-05. Do not create a parallel review/submission payload.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-07-01: Review and hand off one exact product campaign</name>
  <files>apps/web/src/features/create/CampaignReviewStep.tsx, apps/web/src/features/create/CampaignReviewStep.test.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creator.css, apps/web/src/i18n/translations/en.ts</files>
  <behavior>
    - Product review shows source/facts/provenance, campaign, presenter/media, delivery, rights and current server price per D-23/D-24.
    - Each Edit action returns to the owning step and returning to review retains all other fields.
    - Generate opens auth only after rights and ready/unexpired matching quote gates pass per D-25.
  </behavior>
  <action>Write the product interaction first. Build semantic grouped review rows directly from CreationDraft and quote state; do not copy values into local summary state. Implement exact edit routing, sticky/mobile-safe Generate, visible blocked explanation and focus return. Persist the stable pending intent before opening the existing AuthGateDialog.</action>
  <verify><automated>bun run --cwd apps/web test -- src/features/create/CampaignReviewStep.test.tsx -t "exact product review" &amp;&amp; bun run --cwd apps/web typecheck</automated></verify>
  <done>One product campaign reaches Generate-time auth only when exact rights and current quote bindings are valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-07-02: Complete exact Arabic service review</name>
  <files>apps/web/src/features/create/CampaignReviewStep.tsx, apps/web/src/features/create/CampaignReviewStep.test.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/i18n/translations/ar.ts, apps/web/src/features/create/creator.css</files>
  <behavior>
    - Arabic service review preserves business/service facts, provenance, location, booking/WhatsApp, price/offer, presenter/media/rights and delivery.
    - English URLs, numbers, KWD and names remain direction-isolated in RTL.
    - Editing any quote-affecting value returns with quote loading and no stale price.
  </behavior>
  <action>Add the Arabic service and edit-return matrix before code. Complete localized whole-string copy and logical layout, including Not added/provenance/status text. Assert changed settings invalidate the old quote, the updated server response is the only displayed price and stale quote IDs never reach pending submission.</action>
  <verify><automated>bun run --cwd apps/web test -- src/features/create/CampaignReviewStep.test.tsx -t "Arabic service review" &amp;&amp; bun run --cwd apps/web typecheck</automated></verify>
  <done>The Arabic service review is complete, RTL-correct and cannot display or submit a stale quote.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-07-03: Preserve exact review through quote outages and authentication recovery</name>
  <files>apps/web/src/features/create/CampaignReviewStep.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/GoldenPathStates.test.tsx, apps/web/src/features/create/AuthGateDialog.test.tsx</files>
  <behavior>
    - Quote loading/unavailable/expired/changed, offline, worker/capability unavailable and upload failure keep the review and expose one retry/edit action per D-26/D-29.
    - Cancelled auth returns focus to Generate and leaves the exact review unchanged.
    - Replayed/second callback reuses the same pending intent and cannot create a duplicate submission.
  </behavior>
  <action>Write the recovery table first. Use the quote/service availability distinctions from existing API errors, polite status for loading, alert for actionable failure and expandable request ID. Reuse Phase 2 claim/auth state machine exactly; do not auto-retry claims/submission while offline or delete local blobs before verified receipt.</action>
  <verify><automated>bun run --cwd apps/web test -- src/features/create/GoldenPathStates.test.tsx src/features/create/AuthGateDialog.test.tsx src/features/create/CampaignReviewStep.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated></verify>
  <done>Every quote/auth/service failure preserves the exact review and repeated callbacks cannot duplicate claim or submission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical draft to rendered review | Displayed summary must not diverge from submitted data. |
| Guest review through auth callback | Pending intent and account ownership cross browser/server session boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-03 | Tampering | Review/submission divergence | high | mitigate | Direct canonical derivation, exact deep-equality tests and no duplicate payload. |
| T-03-05 | Tampering | Stale quote | high | mitigate | Ready/unexpired matching quote gate and invalidation on all relevant edits. |
| T-03-08 | Repudiation | Auth/callback replay | high | mitigate | Stable pending ID, Phase 2 digest/idempotency and replay tests. |
</threat_model>

<verification>
- Product, Arabic service, quote-state and auth recovery tests pass.
- Review and pending submission deeply match the canonical draft.
- No stale/fallback price enables Generate.
</verification>

<success_criteria>
- Product and service reviews are exact and understandable.
- Generate-time auth preserves one pending campaign exactly once.
- No high-severity review/auth threat remains unresolved.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-07-SUMMARY.md` when done.
</output>
