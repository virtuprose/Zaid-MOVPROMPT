---
phase: 03-product-and-service-golden-paths
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/features/create/SourceChoiceStep.tsx
  - apps/web/src/features/create/FactReviewStep.tsx
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/creator.css
  - apps/web/src/features/create/GoldenPathFlow.test.tsx
  - apps/web/src/features/create/FactReviewStep.test.tsx
  - apps/web/src/features/create/GoldenPathStates.test.tsx
  - apps/web/src/i18n/translations/en.ts
  - apps/web/src/i18n/translations/ar.ts
autonomous: true
requirements: [SOURCE-01, SOURCE-02, SOURCE-03, CREATE-01, CREATE-10]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-01-SUMMARY.md
  - .planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/CreatorProgress.tsx
  - apps/web/src/lib/api/portableApiClient.ts
artifacts:
  - path: apps/web/src/features/create/SourceChoiceStep.tsx
    provides: Four truthful customer source entry choices
  - path: apps/web/src/features/create/FactReviewStep.tsx
    provides: Source-specific fact review with visible provenance
  - path: apps/web/src/features/create/GoldenPathFlow.test.tsx
    provides: Product/service source-to-confirmation component proof
acceptance_criteria:
  - The first screen asks What are you promoting and contains no customer-facing sample source.
  - Link, upload/footage, and manual paths retain all values while reaching fact review.
  - Product and service fact review show the right fields, provenance, inline validation, and Not added states.
  - Source loading, cancellation, scan/upload failure, and offline states preserve the complete draft.
must_haves:
  truths:
    - A beginner can add a real product or service without seeing generation jargon.
    - Imported facts cannot pass to recommendations until the user reviews them.
    - Every correction stays visible and survives any recoverable source error.
  artifacts:
    - path: apps/web/src/features/create/SourceChoiceStep.tsx
      provides: Accessible source selection/import/upload/manual UI
    - path: apps/web/src/features/create/FactReviewStep.tsx
      provides: Controlled normalized fact editor
    - path: apps/web/src/features/create/GoldenPathStates.test.tsx
      provides: Source-state retention and recovery checks
  key_links:
    - from: apps/web/src/features/create/SourceChoiceStep.tsx
      to: apps/web/src/lib/api/portableApiClient.ts
      via: Existing internal scanSource method only for URL sources
      pattern: portableCreatorApi.*scan
    - from: apps/web/src/features/create/FactReviewStep.tsx
      to: apps/web/src/features/create/sourceFacts.ts
      via: Controlled edit and confirm callbacks
      pattern: editFact|confirmCampaignFacts
---

<objective>
Replace the current broad source area with the complete D-05 through D-11 source and fact-confirmation journey while retaining Phase 2 exact draft recovery.

Purpose: Users must understand what to add, see where each business fact came from, and correct it without losing work.
Output: Focused source/fact steps, source-specific validation, complete interaction states, bilingual copy, and component tests.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
@.planning/phases/03-product-and-service-golden-paths/03-PATTERNS.md
@.planning/phases/03-product-and-service-golden-paths/03-01-SUMMARY.md

<interfaces>
SourceChoiceStep and FactReviewStep are controlled components. CreateStudio remains the only owner of route entry, abortable source scan, guest save/recovery, quote, auth, claim, and submit. The components consume CampaignSource and sourceFacts helpers from Plan 03-01 and never create a second scanner or store.

API coverage declaration: this plan uses the existing internal MovPrompt source-scan API and PortableApiError envelope. It adds no external API and does not create COVERAGE.md.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-02-01: Complete product-link to confirmed-fact review</name>
  <files>apps/web/src/features/create/SourceChoiceStep.tsx, apps/web/src/features/create/FactReviewStep.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creator.css, apps/web/src/features/create/GoldenPathFlow.test.tsx</files>
  <behavior>
    - The first screen asks the D-05 question and exposes four labelled source choices with no customer-facing sample action.
    - A product URL stays visible during the existing abortable scan, then maps returned values to imported facts and opens fact review.
    - Editing a returned value immediately shows Added by you; Confirm details changes untouched campaign-used facts to Confirmed by you.
  </behavior>
  <action>Write the failing product journey first, then extract controlled SourceChoiceStep and FactReviewStep from CreateStudio. Wire Product link through portableCreatorApi.scanSource and the Plan 03-01 fact helpers; keep CreateStudio as controller per the established callback pattern. Implement the D-03 one-job/one-primary-action hierarchy and the D-07/D-08 provenance labels. Use the existing creator tokens, semantic controls, and logical CSS per D-27/D-28. The import result is always reviewable data, never trusted campaign copy.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/GoldenPathFlow.test.tsx -t "product link to confirmed facts" &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>A product link reaches dedicated fact review through the hardened internal scanner, and fact edits/confirmation update provenance without changing unrelated facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-02: Complete service, upload, footage, and manual fact review</name>
  <files>apps/web/src/features/create/SourceChoiceStep.tsx, apps/web/src/features/create/FactReviewStep.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/FactReviewStep.test.tsx, apps/web/src/features/create/creator.css</files>
  <behavior>
    - Business/service URL and manual entry prioritize service name, facts, location, booking link, WhatsApp, price/offer, brand, and real media per D-09.
    - Photo upload and real footage remain source assets; footage never changes presenter state per D-11.
    - Optional missing facts read Not added; outcome-required validation is adjacent, focuses the first invalid field, and never clears values per D-10.
  </behavior>
  <action>Create the failing service/manual/upload cases before expansion. Add the remaining source branches to the same controlled steps and save every transition through the existing IndexedDB draft path. Keep product and service as typed views of the normalized source, not alternate state objects. Preserve the D-11 boundary between source footage and presenter selection, use the UI-SPEC source-specific field order, and keep the production sample path absent.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/FactReviewStep.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>All five source methods reach the correct source-specific fact editor with the exact source, facts, media keys, and provenance preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-03: Add bilingual source validation and non-destructive recovery states</name>
  <files>apps/web/src/features/create/SourceChoiceStep.tsx, apps/web/src/features/create/FactReviewStep.tsx, apps/web/src/features/create/GoldenPathStates.test.tsx, apps/web/src/i18n/translations/en.ts, apps/web/src/i18n/translations/ar.ts</files>
  <behavior>
    - Loading, cancel, scan/upload error, and offline states each expose one clear recovery action and preserve the normalized source per D-26.
    - Optional missing facts read Not added; outcome-required validation is adjacent, focuses the first invalid field, and never clears values per D-10.
    - English and Arabic source/provenance/recovery labels are complete, with native group semantics, linked errors, status announcements, 44px targets, true RTL, and reduced motion per D-27 through D-29.
  </behavior>
  <action>Write failing recovery, validation, and Arabic cases first. Add complete English and Arabic strings rather than assembling fragments, associate errors with their fields, announce async states politely, and preserve the current values through every retry or cancellation. Use native groups and logical layout, and ensure the D-26 primary recovery plus safe escape never deletes the draft or changes provenance.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/GoldenPathStates.test.tsx src/features/create/FactReviewStep.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Every source validation/error/offline state is accessible and bilingual, retains the exact draft, and offers one correct recovery action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to internal source-scan API | Untrusted public URL and remote page metadata enter the creator. |
| Source response to campaign facts | Imported values become editable, provenance-bearing draft data only after review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Information disclosure | URL source import | high | mitigate | Reuse the hardened internal scanner, abort controller, no-store response, public-network policy, and bounded error envelope; no browser fetch. |
| T-03-02 | Tampering | Fact review | high | mitigate | Visible provenance, explicit confirmation, immutable edits, text rendering, and field-level tests prevent unreviewed imported claims. |
| T-03-08 | Denial of service / Repudiation | Scan, upload, and offline recovery | medium | mitigate | Preserve the exact IndexedDB draft, keep cancellation safe, and expose one retry/fallback with request ID support. |
</threat_model>

<verification>
- Product, service, upload, footage, and manual component journeys pass targeted tests.
- English and Arabic source/fact copy is complete; no provider/model/prompt/timeline text is rendered.
- Keyboard roles, labels, error associations, live status, and recovery retention are asserted.
</verification>

<success_criteria>
- SOURCE-01, SOURCE-02, and SOURCE-03 are usable through the rendered creator.
- CREATE-01 beginner safety and CREATE-10 interaction-state rules hold from source choice through fact confirmation.
- No new network or storage trust boundary is introduced.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-02-SUMMARY.md` when done.
</output>
