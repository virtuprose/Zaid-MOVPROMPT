---
phase: 03-product-and-service-golden-paths
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/creator.ts
  - packages/contracts/src/contracts.test.ts
  - packages/creative-engine/src/types.ts
  - apps/web/src/features/create/types.ts
  - apps/web/src/features/create/contracts.ts
  - apps/web/src/features/create/sourceFacts.ts
  - apps/web/src/features/create/campaignFacts.test.ts
  - apps/web/src/features/create/projectStore.ts
  - apps/web/src/features/create/portableProjectMapper.ts
  - apps/web/src/features/create/campaignDraft.test.ts
autonomous: true
requirements: [SOURCE-01, SOURCE-02, SOURCE-03, CREATE-02, CREATE-03]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
  - .planning/phases/03-product-and-service-golden-paths/03-RESEARCH.md
  - .planning/phases/03-product-and-service-golden-paths/03-PATTERNS.md
  - packages/contracts/src/creator.ts
  - apps/web/src/features/create/contracts.ts
  - apps/web/src/features/create/projectStore.ts
artifacts:
  - path: packages/contracts/src/creator.ts
    provides: Shared CampaignSource and ConfirmedFact collection contract
  - path: apps/web/src/features/create/sourceFacts.ts
    provides: Immutable provenance and outcome-requirement transitions
  - path: apps/web/src/features/create/campaignDraft.test.ts
    provides: Product/service and source-first/template-first round-trip proof
acceptance_criteria:
  - Product and service inputs normalize into one CampaignSource with one ConfirmedFact collection.
  - Imported edits and explicit confirmation produce the exact locked provenance transitions.
  - All nine campaign outcomes survive draft, claim configuration, project-version, quote, and hydration mappings.
  - Source-first and template-first inputs converge without losing source facts, assets, or the immutable template choice.
must_haves:
  truths:
    - A product or service is represented by the same normalized source/fact anchor rather than parallel authoritative models.
    - Every campaign fact retains its value and provenance across browser draft, canonical configuration, and cloud hydration.
    - Identical source-first and template-first campaign intent produces equivalent normalized configuration.
  artifacts:
    - path: packages/contracts/src/creator.ts
      provides: Strict Zod source, fact, outcome, and presenter boundary
      contains: CampaignSourceSchema
    - path: apps/web/src/features/create/sourceFacts.ts
      provides: Pure fact update, confirmation, and requirement helpers
      exports: [applyImportedFacts, editFact, confirmCampaignFacts, requiredFactsForOutcome]
    - path: apps/web/src/features/create/campaignDraft.test.ts
      provides: Cross-boundary round-trip and convergence regression suite
  key_links:
    - from: apps/web/src/features/create/contracts.ts
      to: packages/contracts/src/creator.ts
      via: CreationDraft carries the shared CampaignSource and ConfirmedFact types
      pattern: CampaignSource
    - from: apps/web/src/features/create/projectStore.ts
      to: apps/web/src/features/create/portableProjectMapper.ts
      via: canonical project configuration serializes and hydrates the same normalized source
      pattern: source.*facts
---

<objective>
Make CampaignSource plus its ConfirmedFact collection the normalized anchor for every product and service campaign per D-06, with product/service retained only as typed source variants. Carry that anchor through the existing browser draft, claim/version configuration, quote input, and cloud hydration seams.

Purpose: The golden paths cannot be truthful if facts or provenance disappear at authentication, quoting, or reload.
Output: Shared schemas, immutable fact transitions, expanded outcome taxonomy, and round-trip regression proof.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-RESEARCH.md
@.planning/phases/03-product-and-service-golden-paths/03-PATTERNS.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-02-SUMMARY.md

<interfaces>
Existing boundaries to extend: CampaignSourceKindSchema, CampaignGoalSchema, ConfirmedFactSchema, CreationDraft, projectToCreationDraft, buildPortableGenerationConfiguration, portableProductRecipe, portableConfiguration, portableCampaignRecipe, stableProjectConfiguration, and projectFromCloud.

The browser/API anchor is CampaignSource with ConfirmedFact[]; product and service are discriminated source variants, never separate persisted truth models. Phase 3 calls only existing internal MovPrompt APIs and introduces no external API integration or COVERAGE.md obligation.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-01-01: Prove one manual-product fact through the normalized contract</name>
  <reversibility rating="costly">D-06 changes a cross-module serialized contract used by browser recovery, API validation, project versions, and quote hashing.</reversibility>
  <files>packages/contracts/src/creator.ts, packages/contracts/src/contracts.test.ts, apps/web/src/features/create/types.ts, apps/web/src/features/create/sourceFacts.ts, apps/web/src/features/create/campaignFacts.test.ts</files>
  <behavior>
    - A manual product name and image asset key parse as one CampaignSource and round-trip through the pure normalized source mapper unchanged.
    - The normalized object contains stable asset identifiers and fact values only; blob URLs, remote preview URLs, and signed URLs are rejected or omitted.
    - CampaignSource/ConfirmedFact is authoritative; the legacy product view can be derived but cannot introduce a second value for the same fact.
  </behavior>
  <action>Write the failing product tracer first. Extend the strict Zod contract with CampaignSourceSchema and the bounded fact fields needed by D-09, while preserving the existing three provenance wire values. Add the normalized source to CreatorProject and CreationDraft and implement the thinnest pure mapping in sourceFacts.ts. Preserve D-01/D-02 beginner terminology outside the contract, and keep all provider identifiers out of public types. Do not add a database migration because project versions already persist validated JSON configuration.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/campaignFacts.test.ts -t "manual product tracer" &amp;&amp; bun run --cwd packages/contracts test -- contracts &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>A manual product source is accepted by the shared schema, normalized through the web helper, and restored with the same fact and stable asset key under focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-02: Expand all source variants and provenance transitions</name>
  <files>packages/contracts/src/creator.ts, apps/web/src/features/create/types.ts, apps/web/src/features/create/sourceFacts.ts, apps/web/src/features/create/campaignFacts.test.ts</files>
  <behavior>
    - Product URL, business URL, photo upload, real footage, and manual entry each normalize without source-specific data loss per D-05/D-09/D-11.
    - Editing an imported fact changes only that fact to manual provenance per D-08.
    - Confirm details changes only unchanged imported facts used by the campaign to user-confirmed provenance; missing optional fields remain absent and are reported as Not added per D-10.
  </behavior>
  <action>After the failing cases are committed, add immutable applyImportedFacts, editFact, confirmCampaignFacts, factsForReview, and requiredFactsForOutcome helpers. Define source-specific field metadata while persisting only the normalized fact collection. Cover name, description, brand, price, offer, logo/color/media for products and business/service name, service facts, location, booking link, WhatsApp, price/offer, logo/color/media for services per D-07 through D-11. Renderable copy is not stored as fact provenance.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/campaignFacts.test.ts &amp;&amp; bun run --cwd packages/contracts test -- creator &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>All five source methods produce the normalized anchor, and every provenance transition and outcome-aware missing-fact result is deterministic and covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-03: Preserve normalized intent through convergence, quote configuration, and hydration</name>
  <files>packages/creative-engine/src/types.ts, apps/web/src/features/create/contracts.ts, apps/web/src/features/create/projectStore.ts, apps/web/src/features/create/portableProjectMapper.ts, apps/web/src/features/create/campaignDraft.test.ts</files>
  <behavior>
    - The nine D-12 outcomes validate in both public contracts and the creative brief without aliases or relabeling.
    - Source-first confirmed facts remain authoritative when a template is chosen; template-first retains the immutable template while gathering its missing source inputs per D-14.
    - Product and service configurations survive projectToCreationDraft, canonical guest snapshot input, buildPortableGenerationConfiguration, portable claim recipes, and projectFromCloud with exact source/fact equality.
  </behavior>
  <action>Start with the failing product/service convergence matrix. Extend CreativeBriefSchema to the same campaign outcome enum, then update buildPortableGenerationConfiguration, portableProductRecipe, portableConfiguration, portableCampaignRecipe, stableProjectConfiguration, and projectFromCloud to serialize the normalized source once. Keep legacy CreatorProduct derivation at the compatibility edge only. Bind quote-affecting settings to this canonical object so a changed fact invalidates the configuration hash through the existing quote service. Preserve the D-14 rule that template choice cannot silently rewrite confirmed facts or CTA intent.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/campaignDraft.test.ts &amp;&amp; bun run --cwd packages/creative-engine test -- types &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Both entry orders and both source families round-trip to equivalent canonical configuration, with all outcomes, facts, provenance, assets, template identity, and settings intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted source and browser draft to shared contract | Imported/manual values cross into campaign truth and later quote hashing. |
| Guest draft to claim/project version | Local source facts become owner-scoped durable configuration after authentication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | CampaignSource and source facts | high | mitigate | Strict bounded Zod schemas, immutable provenance transitions, and no URL-bearing media in canonical facts. |
| T-03-02 | Tampering / Repudiation | Fact confirmation | high | mitigate | Persist explicit provenance per fact and test edit/confirm transitions independently of visual state. |
| T-03-03 | Tampering / Information disclosure | Draft, claim, and quote serialization | high | mitigate | One canonical serializer, exact round-trip tests, stable asset keys only, and existing owner-scoped claim boundary. |
</threat_model>

<verification>
- Focused contract/fact/draft tests pass before later UI plans consume the new interfaces.
- `bun run --cwd packages/contracts test -- creator`, `bun run --cwd packages/creative-engine test -- types`, and web typecheck pass.
- The test fixtures contain no signed URL, blob URL, provider ID, or duplicate product/service truth field.
</verification>

<success_criteria>
- SOURCE-01 through SOURCE-03 have a single validated, provenance-bearing representation.
- CREATE-02 convergence is byte-equivalent after canonical serialization.
- CREATE-03 has one shared nine-outcome taxonomy across web and creative engine.
- No unresolved high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-01-SUMMARY.md` when done.
</output>
