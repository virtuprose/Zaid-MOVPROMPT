---
phase: 03-product-and-service-golden-paths
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - apps/web/src/features/create/OutcomeStep.tsx
  - apps/web/src/features/create/templateRecommendations.ts
  - apps/web/src/features/create/templateRecommendations.test.ts
  - apps/web/src/features/create/TemplateRecommendations.tsx
  - apps/web/src/features/create/TemplateRecommendation.test.tsx
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/TemplateGrid.tsx
  - apps/web/src/features/create/templateMedia.ts
  - apps/web/src/features/create/TemplateGrid.test.tsx
  - apps/web/src/features/create/creator.css
autonomous: true
requirements: [CREATE-02, CREATE-03, CREATE-04, CREATE-05, CREATE-06, CREATE-10]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-04-SUMMARY.md
  - .planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
  - apps/web/src/features/create/useTemplateQuotes.ts
  - apps/web/src/features/create/TemplateGrid.tsx
  - apps/web/src/features/create/templateMedia.ts
artifacts:
  - path: apps/web/src/features/create/templateRecommendations.ts
    provides: Deterministic max-three recommendation scoring with eligibility reasons
  - path: apps/web/src/features/create/TemplateRecommendations.tsx
    provides: Quote-aware recommendation cards and recovery states
  - path: apps/web/src/features/create/TemplateRecommendation.test.tsx
    provides: Disclosure, quote and selection behavior proof
acceptance_criteria:
  - All nine locked outcomes produce deterministic recommendations or one honest recovery state.
  - A recommendation is selectable only when catalog-eligible and its exact server quote is ready/unexpired.
  - Cards render loading, unavailable, expired and changed quote states with retry and no fallback price.
  - Only verified template-specific motion is playable; static directions are never styled as video.
must_haves:
  truths:
    - A beginner understands why each of at most three templates fits the chosen business outcome.
    - Selection cannot outrun a pending, failed, expired or changed authoritative quote.
    - Static artwork is visibly and technically non-playable.
  artifacts:
    - path: apps/web/src/features/create/TemplateRecommendations.tsx
      provides: Accessible recommendation and quote-state surface
    - path: apps/web/src/features/create/TemplateGrid.tsx
      provides: Verified-motion/static-direction affordance boundary
  key_links:
    - from: apps/web/src/features/create/TemplateRecommendations.tsx
      to: apps/web/src/features/create/useTemplateQuotes.ts
      via: Each candidate consumes quote state keyed by template/configuration
      pattern: quoteState
    - from: apps/web/src/features/create/TemplateGrid.tsx
      to: apps/web/src/features/create/templateMedia.ts
      via: Verified previewVideo is the sole Play gate
      pattern: previewVideo
---

<objective>
Render D-12 through D-17 as beginner-safe outcome and recommendation screens backed by catalog eligibility and authoritative quote state.

Purpose: Users should compare relevant templates without being shown a selectable option whose price or media truth is unknown.
Output: Outcome selection, explainable recommendations, quote-state cards and truthful media previews.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md

<interfaces>
Consume the normalized CampaignSource/ConfirmedFact collection from Plan 03-01, published catalog eligibility and per-template quote lifecycle from Plan 03-03, and server presenter constraints from Plan 03-04. No customer-facing provider, model, prompt or timeline vocabulary is permitted.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-05-01: Recommend one quoted template for one outcome</name>
  <files>apps/web/src/features/create/OutcomeStep.tsx, apps/web/src/features/create/templateRecommendations.ts, apps/web/src/features/create/templateRecommendations.test.ts, apps/web/src/features/create/TemplateRecommendations.tsx, apps/web/src/features/create/TemplateRecommendation.test.tsx</files>
  <behavior>
    - One confirmed product outcome yields a stable top recommendation and a plain-language why-this-fits explanation per D-12/D-13.
    - Its card remains disabled while quote state is loading and becomes selectable only when the matching server quote is ready/unexpired.
    - Selection emits immutable template version plus matching quote binding without rewriting confirmed source facts per D-14.
  </behavior>
  <action>Start with the single-path interaction test. Implement the pure scorer with stable tie-breaks from published catalog facts only. Wire OutcomeStep and TemplateRecommendations through controlled props so candidate quote state is consumed and selection emits the exact template/quote pair only when eligibility and quote readiness are true. Use the UI-SPEC heading, step purpose, single primary action and textual selected/disabled states.</action>
  <verify><automated>bun run --cwd apps/web test -- src/features/create/templateRecommendations.test.ts src/features/create/TemplateRecommendation.test.tsx -t "quoted recommendation tracer" &amp;&amp; bun run --cwd apps/web typecheck</automated></verify>
  <done>One outcome reaches one explainable recommendation that can be selected only with its exact authoritative quote.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-05-02: Complete recommendation disclosures and quote recovery states</name>
  <files>apps/web/src/features/create/TemplateRecommendations.tsx, apps/web/src/features/create/TemplateRecommendation.test.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creator.css</files>
  <behavior>
    - All outcomes return at most three ranked cards or one actionable empty result per D-13.
    - Every card discloses expected result, required inputs, duration, formats, presenter compatibility, preview type and current server quote state per D-15.
    - Loading, unavailable, expired and changed quotes show exact retry/review action; no credit number is shown outside ready state.
  </behavior>
  <action>Add zero/one/two/three, ineligible and every quote-state test first. Connect the controlled recommendation surface to CreateStudio/useTemplateQuotes and persist only the emitted exact template/quote pair. Render one calm vertical card hierarchy at mobile and multi-column when space permits, with 44px controls, visible focus, logical RTL and polite status updates. Use server request ID only in expandable support details. Retry the exact quote; do not silently substitute another template or cached price.</action>
  <verify><automated>bun run --cwd apps/web test -- src/features/create/TemplateRecommendation.test.tsx src/features/create/templateRecommendations.test.ts &amp;&amp; bun run --cwd apps/web typecheck</automated></verify>
  <done>All recommendations and quote failures are truthful, accessible and recoverable without losing the selected outcome or source facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-05-03: Preserve verified-motion and static-direction truth</name>
  <files>apps/web/src/features/create/TemplateGrid.tsx, apps/web/src/features/create/templateMedia.ts, apps/web/src/features/create/TemplateRecommendation.test.tsx, apps/web/src/features/create/TemplateGrid.test.tsx, apps/web/src/features/create/creator.css</files>
  <behavior>
    - Play exists only for verified template-specific motion; static direction exposes View direction and no player-shaped chrome per D-16.
    - The existing preview dialog traps/returns focus, labels close, handles media loading/unavailable and pauses when closed.
    - Reduced motion removes nonessential transitions without hiding state changes.
  </behavior>
  <action>Write media-truth and dialog cases before changes. Keep previewVideo as the sole Play gate and prevent duration bars/generated-example wording on static art. Reuse the existing dialog primitive and token system; make only UI-SPEC-driven responsive, focus and reduced-motion corrections.</action>
  <verify><automated>bun run --cwd apps/web test -- src/features/create/TemplateRecommendation.test.tsx src/features/create/TemplateGrid.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated></verify>
  <done>Verified previews play accessibly and every unverified/static direction remains clearly non-playable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server catalog/quotes to selection UI | UI may display but cannot upgrade eligibility or quote state. |
| Template media to browser | Only verified motion may instantiate a video player. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-05 | Tampering | Recommendation selection | high | mitigate | Selection requires catalog eligible plus ready quote with matching template/configuration key. |
| T-03-13 | Spoofing | Template preview | medium | mitigate | Verified previewVideo sole Play gate and explicit static-direction UI/tests. |
| T-03-14 | Denial of service | Quote fan-out/races | medium | mitigate | Max-three candidates, request cancellation, deduplication and explicit retry. |
</threat_model>

<verification>
- Recommendation, quote-state and media tests pass.
- No card renders price from catalog/client fallback.
- No unverified/static card exposes Play semantics.
</verification>

<success_criteria>
- Every outcome and recommendation state is understandable without technical knowledge.
- Selection always carries an eligible immutable template and current server quote.
- No high-severity selection threat remains unresolved.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-05-SUMMARY.md` when done.
</output>
