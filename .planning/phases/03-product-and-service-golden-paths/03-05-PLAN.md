---
phase: 03-product-and-service-golden-paths
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - apps/web/src/features/create/CampaignReviewStep.tsx
  - apps/web/src/features/create/CampaignReviewStep.test.tsx
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/GoldenPathStates.test.tsx
  - apps/web/src/features/create/AuthGateDialog.test.tsx
  - apps/web/src/features/create/creator.css
  - apps/web/src/i18n/translations/en.ts
  - apps/web/src/i18n/translations/ar.ts
autonomous: true
requirements: [SOURCE-02, SOURCE-03, CREATE-02, CREATE-05, CREATE-07, CREATE-08, CREATE-09, CREATE-10]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/03-product-and-service-golden-paths/03-04-SUMMARY.md
  - .planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/AuthGateDialog.tsx
  - apps/web/src/features/create/guestClaimRecovery.ts
artifacts:
  - path: apps/web/src/features/create/CampaignReviewStep.tsx
    provides: Six-group exact campaign review and edit routing
  - path: apps/web/src/features/create/CampaignReviewStep.test.tsx
    provides: Review completeness and quote/rights gating proof
  - path: apps/web/src/features/create/GoldenPathStates.test.tsx
    provides: Saved-draft recovery matrix through Generate-time auth
acceptance_criteria:
  - Review derives from the normalized draft and shows every generation-used fact plus provenance.
  - Source, Campaign, Presenter and media, Delivery, Rights, and Price each have one exact Edit action.
  - Generate is enabled only with complete rights and a valid unexpired authoritative quote.
  - Quote, worker, offline, upload, capability, and cancelled-auth states retain the exact draft and expose one recovery action.
must_haves:
  truths:
    - A user can verify the complete campaign in a calm summary before authentication.
    - The displayed review, claimed version, and quoted configuration are the same data, not three summaries.
    - Cancelling auth or retrying price returns to the unchanged review and correct focus.
  artifacts:
    - path: apps/web/src/features/create/CampaignReviewStep.tsx
      provides: Semantic grouped review with exact-step edit actions
    - path: apps/web/src/features/create/CampaignReviewStep.test.tsx
      provides: Product/service review and gating assertions
    - path: apps/web/src/features/create/GoldenPathStates.test.tsx
      provides: Recovery-state retention assertions
  key_links:
    - from: apps/web/src/features/create/CampaignReviewStep.tsx
      to: apps/web/src/features/create/contracts.ts
      via: Review rows are derived directly from CreationDraft/CampaignSource
      pattern: source.*facts|campaign
    - from: apps/web/src/features/create/CreateStudio.tsx
      to: apps/web/src/features/create/AuthGateDialog.tsx
      via: Existing Generate-time auth intent only after quote and rights gates
      pattern: pendingGeneration|AuthGateDialog
---

<objective>
Complete D-23 through D-26 with one exact final review and reuse the existing quote, Generate-time auth, claim, and submission path without a parallel payload.

Purpose: The user must know exactly what will be created, why Generate is available or blocked, and that every recovery keeps the campaign safe.
Output: Grouped review, exact edit routing, authoritative price/rights gates, resilient recovery states, and auth-handoff tests.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-product-and-service-golden-paths/03-CONTEXT.md
@.planning/phases/03-product-and-service-golden-paths/03-UI-SPEC.md
@.planning/phases/03-product-and-service-golden-paths/03-RESEARCH.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-08-SUMMARY.md
@.planning/phases/03-product-and-service-golden-paths/03-04-SUMMARY.md

<interfaces>
CampaignReviewStep receives the current normalized CreationDraft/project, actual selected template display model, authoritative GenerationQuote state, rights state, and onEdit(step) callbacks. It never constructs a second submission object. CreateStudio keeps the existing quote refresh, pending intent, AuthGateDialog, claim recovery, and start-generation handlers.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-05-01: Review and hand off one exact product campaign</name>
  <files>apps/web/src/features/create/CampaignReviewStep.tsx, apps/web/src/features/create/CampaignReviewStep.test.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creator.css, apps/web/src/i18n/translations/en.ts</files>
  <behavior>
    - The product review has D-23 six groups and shows the D-24 template/media truth, outcome, source, every used fact/provenance, language, CTA, price/offer, presenter, delivery, rights, and quote.
    - Each group has one Edit action that returns to the exact step and preserves review scroll/state.
    - Generate campaign is enabled only with required rights and a valid unexpired quote; clicking it uses the existing auth intent/claim/submission path.
  </behavior>
  <action>Write the failing product review tracer first. Build CampaignReviewStep as semantic grouped output derived directly from the normalized draft plus live template/quote view models. Wire exact Edit callbacks to CreateStudio and replace the broad details summary. Reuse the existing Generate handler, AuthGateDialog, guest claim, quote confirmation, and start submission; do not create a separate review or submission payload. Use the D-23 calm hierarchy, one group Edit action, definition-list semantics, and one amber primary action.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/CampaignReviewStep.test.tsx -t "exact product review" &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>The displayed product review equals the submitted canonical configuration field-for-field and hands off only through the existing Generate-time auth path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-05-02: Complete exact Arabic service review</name>
  <files>apps/web/src/features/create/CampaignReviewStep.tsx, apps/web/src/features/create/CampaignReviewStep.test.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/i18n/translations/ar.ts, apps/web/src/features/create/creator.css</files>
  <behavior>
    - Service review includes booking/WhatsApp/location/service facts and exact provenance while omitting no generation-used value.
    - Arabic review uses true RTL and direction-safe price, phone, URL, and CTA values with complete strings.
  </behavior>
  <action>Commit the failing service-review cases first. Expand review rows and complete Arabic copy, preserving the same six semantic groups and exact Edit targets used by products. Render KWD, phone, URL, booking, WhatsApp, and CTA values in direction-safe spans. Apply D-27 through D-29 focus, RTL, target, wrapping, and reduced-motion rules without changing the canonical draft.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/CampaignReviewStep.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Product and service reviews are complete, lossless, and semantically equivalent in English and true Arabic RTL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-05-03: Preserve exact review through quote, outage, and auth recovery</name>
  <files>apps/web/src/features/create/CampaignReviewStep.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/GoldenPathStates.test.tsx, apps/web/src/features/create/AuthGateDialog.test.tsx</files>
  <behavior>
    - Price loading, retry, changed/expired quote, worker pause, offline, capability unavailable, upload failure, and incomplete rights each retain the full draft and expose one D-25/D-26 recovery action.
    - Cancelled authentication closes the dialog, restores focus to Generate campaign, and leaves template, source, facts, settings, rights, quote intent, and local assets unchanged.
    - Generate remains disabled with a named reason until rights and authoritative quote are both current.
  </behavior>
  <action>Write the failing recovery matrix first, then connect the existing quote and auth discriminated states to the exact UI-SPEC messages/actions. Preserve stable pendingGenerationId across retries and auth cancellation, retain Phase 2 cleanup guards, and announce status changes politely. Never simulate provider progress or success, and never enable Generate from a client fallback price.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/GoldenPathStates.test.tsx src/features/create/AuthGateDialog.test.tsx src/features/create/CampaignReviewStep.test.tsx &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Every blocked/retry/cancel path preserves the exact campaign and offers one accessible recovery action while Generate fails closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser review to quote/auth/claim | User-visible facts and settings become a priced, authenticated immutable configuration. |
| Auth cancellation/callback to local draft | External auth navigation returns control to a same-browser pending intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering / Repudiation | Review versus submitted configuration | high | mitigate | Derive review and submit from one normalized draft; exact equality tests cover product and service paths. |
| T-03-07 | Tampering | Quote and rights gating | high | mitigate | Fail closed on missing/expired quote or rights and require existing server quote confirmation path. |
| T-03-08 | Denial of service / Repudiation | Retry, offline, worker, and auth cancellation | medium | mitigate | Stable pending intent, exact IndexedDB draft retention, focus restoration, and one non-destructive recovery action. |
</threat_model>

<verification>
- Product/service review tests assert every displayed and submitted value and every Edit target.
- Quote/rights/auth recovery tests prove Generate gates and exact draft retention.
- English/Arabic semantic grouping, focus return, status announcements, and RTL-safe business values pass component checks.
</verification>

<success_criteria>
- CREATE-09 is exact, complete, and authoritative.
- CREATE-10 review/auth/recovery states are usable without losing the draft.
- SOURCE-02/SOURCE-03 facts and provenance remain visible through submission.
- No unresolved high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/03-product-and-service-golden-paths/03-05-SUMMARY.md` when done.
</output>
