---
phase: 01-production-truth-foundation
plan: "03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/contracts/src/generation.ts
  - apps/api/src/generation-repository.ts
  - apps/api/src/generation-service.ts
  - apps/worker/src/render-lifecycle.ts
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/creator.css
  - apps/web/src/pages/CreatorProjects.tsx
autonomous: true
requirements: [TRUTH-02, TRUTH-03, TRUTH-04, TRUTH-07]
user_setup: []
must_haves:
  truths:
    - Progress is reconstructed from persisted named server stages after reload.
    - Completed/failed provider state cannot leave the canonical UI at a fabricated percentage.
    - Generated output is displayed only from an accepted MovPrompt-owned artifact.
    - Every failure keeps the project and last accepted output recoverable.
  artifacts:
    - apps/worker/src/render-lifecycle.ts
    - apps/web/src/features/create/CreateStudio.tsx
    - apps/web/src/pages/CreatorProjects.tsx
  key_links:
    - Worker stage transitions persist to render_runs and API status projects them.
    - Create and Projects use the same run/status contract.
---

<objective>
Finish durable progress, media truth, recovery, and rendered-browser proof.

Purpose: eliminate stuck 92% screens and demo media leakage from the client journey.
Output: persisted stage projection, honest recovery actions, project run visibility, and accessibility/browser evidence.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-production-truth-foundation/01-CONTEXT.md
@.planning/phases/01-production-truth-foundation/01-UI-SPEC.md
@.planning/phases/01-production-truth-foundation/01-02-SUMMARY.md
@apps/web/src/features/create/CreateStudio.tsx
@apps/worker/src/render-lifecycle.ts
</context>

<threat_model>
- Threat: browser completes a run without worker proof. Mitigation: browser is read-only projection of persisted stage/status/output.
- Threat: demo/sample URL is accepted as generated output. Mitigation: output must be tied to accepted run/version and owned object key.
- Threat: support details leak raw provider errors. Mitigation: stable safe code plus request ID only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile persisted stage transitions and old stuck runs</name>
  <files>packages/contracts/src/generation.ts, apps/api/src/generation-repository.ts, apps/api/src/generation-service.ts, apps/worker/src/render-lifecycle.ts</files>
  <read_first>packages/db/src/schema.ts, apps/worker/src/output-persister.ts, apps/worker/src/output-quality-reviewer.ts</read_first>
  <action>Audit every lifecycle path so preparing, rendering, securing_output, quality_review, ready, cancelling, failed, and cancelled are persisted. Ensure retry/reconcile operates on the existing provider operation, never creates a new paid request, and terminal states update project status while preserving the last accepted version. Add owner-safe run listing/recovery tests for projects.</action>
  <verify>bun test apps/worker/src/render-lifecycle.test.ts apps/worker/src/render-worker.postgres.test.ts apps/api/src/generation-service.test.ts</verify>
  <acceptance_criteria>
    - Provider completion advances beyond rendering or records a precise terminal failure.
    - Reload returns the same run ID and stage.
    - A failed new version leaves currentAcceptedVersionId unchanged.
  </acceptance_criteria>
  <done>Lifecycle and recovery integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove simulated progress and media fallbacks from canonical routes</name>
  <files>apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creatorProjectOutput.ts, apps/web/src/pages/CreatorProjects.tsx, apps/web/src/features/create/creator.css</files>
  <read_first>apps/web/src/features/create/portableProjectMapper.ts, apps/web/src/features/create/templateMedia.ts, apps/web/src/features/create/projectStore.ts</read_first>
  <action>Use only persisted stage labels in production generation. Remove fabricated exact percentages and prevent source, sample, direction, or template-preview media from becoming the editor/output fallback. Keep explicitly labelled client/development preview paths isolated behind development flags. Projects must list all owned runs and distinguish draft, generating, failed, and accepted output.</action>
  <verify>bun test apps/web/src/features/create/creatorProjectOutput.test.ts apps/web/src/features/create/projectStore.test.ts apps/web/src/pages/CreatorProjects.test.tsx</verify>
  <acceptance_criteria>
    - Canonical production state contains no hardcoded 92% progress.
    - Missing accepted output disables Export and shows an honest draft/failure state.
    - All project generations are visible with their real stage/status.
  </acceptance_criteria>
  <done>Web truth-state tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run full runtime and browser acceptance matrix</name>
  <files>apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creator.css, docs/runbooks/GENERATION_ACTIVATION.md</files>
  <read_first>.planning/phases/01-production-truth-foundation/01-VALIDATION.md, docs/runbooks/ENVIRONMENTS.md, compose.yaml</read_first>
  <action>Start the complete local stack without changing paid/provider authorization. Prove health, heartbeat, private storage, feature flags, quote behavior, duplicate submit, reload recovery, project visibility, and failure copy. Run rendered QA at 375/768/1024/1440 in EN/AR and light/dark. Verify keyboard/focus, progress semantics, contrast, reduced motion, no overflow, no dead controls, and clean console. Update the activation runbook with exact non-secret commands and expected states.</action>
  <verify>Workspace lint/typecheck/test/build plus browser accessibility and responsive checks</verify>
  <acceptance_criteria>
    - Healthy dependencies produce ready and an authoritative quote; each removed dependency produces its correct state.
    - Reload and Projects show the same persisted run/stage.
    - Scoped Axe review has no serious/critical violations and no page overflows.
  </acceptance_criteria>
  <done>Database, API, worker, web, and browser evidence agree.</done>
</task>

</tasks>

<verification>
- [ ] Full workspace regression gate passes.
- [ ] Runtime health/heartbeat/storage/quote checks pass.
- [ ] Browser matrix and WCAG 2.2 AA scoped review pass.
- [ ] No paid provider call occurs without separate explicit authorization.
</verification>

<success_criteria>
- Canonical generation state is truthful, durable, recoverable, and accessible.
- Stuck simulated progress and demo-output leakage are absent.
</success_criteria>

<output>After completion, create `01-03-SUMMARY.md`.</output>

