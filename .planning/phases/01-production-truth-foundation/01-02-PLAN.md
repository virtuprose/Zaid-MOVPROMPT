---
phase: 01-production-truth-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/api/src/generation-pricing.ts
  - apps/api/src/generation-service.ts
  - apps/api/src/generation-routes.ts
  - packages/db/src/generation-service.ts
  - packages/auth/src/provisioning.ts
  - packages/contracts/src/generation.ts
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, TRUTH-05]
user_setup: []
must_haves:
  truths:
    - Quotes are expiring, configuration-bound, versioned, and server-priced.
    - Any cost/output-affecting change invalidates the old quote.
    - Repeated submission creates one run and one economic effect.
    - Starter entitlement is provisioned idempotently and consumed only after provider acceptance.
  artifacts:
    - apps/api/src/generation-pricing.ts
    - packages/db/src/generation-service.ts
    - packages/auth/src/provisioning.ts
  key_links:
    - API quote/start uses the canonical DB generation service.
    - Authentication provisioning and render acceptance share the starter entitlement invariant.
---

<objective>
Complete authoritative pricing and exactly-once first-render economics.

Purpose: make the price and starter value credible and prevent duplicate runs/charges.
Output: current quote contract, hash invalidation, idempotent start, and entitlement proof.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-production-truth-foundation/01-CONTEXT.md
@.planning/phases/01-production-truth-foundation/01-RESEARCH.md
@.planning/phases/01-production-truth-foundation/01-01-SUMMARY.md
@packages/db/src/generation-service.ts
@apps/api/src/generation-service.ts
</context>

<threat_model>
- Threat: client changes configuration after receiving a cheaper quote. Mitigation: immutable quote hash and project-version validation inside the start transaction.
- Threat: repeated callbacks/clicks double-charge. Mitigation: owner-scoped idempotency key, advisory lock, unique constraints, and append-only ledger.
- Threat: client claims starter eligibility. Mitigation: server-derived verified-account entitlement and curated template policy.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify complete quote binding</name>
  <files>apps/api/src/generation-pricing.ts, apps/api/src/generation-service.ts, packages/contracts/src/generation.ts</files>
  <read_first>apps/web/src/features/create/projectStore.ts, packages/db/src/generation-policy.ts, packages/creative-engine/src/index.ts</read_first>
  <action>Audit the normalized configuration/hash so template version, source assets/checksums, capability, duration, resolution, delivery ratio, provider canvas ratio, audio, subtitles, language, market, CTA, offer, price, references, and pricing version affect the quote as appropriate. Keep rates server-only integer credits, 15-minute TTL, and resolution-specific pricing. Remove or reject any remaining client fallback.</action>
  <verify>bun test apps/api/src/generation-pricing.test.ts apps/api/src/generation-service.test.ts packages/db/test/generation-service.postgres.test.ts</verify>
  <acceptance_criteria>
    - Identical normalized configurations produce the same hash.
    - Every listed cost/output field change rejects the prior quote.
    - Missing pricing version, TTL, or tier returns pricing_unavailable.
  </acceptance_criteria>
  <done>Quote matrix and hash invalidation tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Prove starter entitlement and idempotent start</name>
  <files>packages/db/src/generation-service.ts, packages/auth/src/provisioning.ts, apps/api/src/generation-routes.ts</files>
  <read_first>packages/db/src/schema.ts, packages/db/migrations/0010_auth_generation_benefits.sql, apps/api/src/generation-repository.ts</read_first>
  <action>Verify provisioning never resets balances or resurrects consumed entitlements. Start must atomically validate ownership/quote/rights, reserve starter or credits, create one run, and enqueue one outbox row. Provider acceptance consumes the entitlement; pre-accept failure releases it; terminal failure refunds exactly once. Add concurrency tests for duplicate Generate and callback replay.</action>
  <verify>Run PostgreSQL auth provisioning and generation service suites with a fresh test database</verify>
  <acceptance_criteria>
    - Concurrent duplicate starts return one render run ID.
    - Ledger, reservation, entitlement, and outbox each show one effect.
    - Failed pre-accept submission restores the starter entitlement exactly once.
  </acceptance_criteria>
  <done>Exactly-once database tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile public quote/start behavior</name>
  <files>apps/api/src/generation-routes.ts, apps/api/src/app.ts, packages/contracts/src/generation.ts</files>
  <read_first>apps/api/src/errors.ts, apps/api/src/request-context.ts, apps/web/src/lib/api/portableApiClient.ts</read_first>
  <action>Ensure quote/start mutation routes require idempotency where needed, use semantic errors and request IDs, reconfirm changed prices, and never expose model IDs or provider usage. Existing run status/cancel/output must remain readable during a quote kill-switch outage.</action>
  <verify>bun test apps/api/src/generation.test.ts apps/api/src/generation-service.test.ts and OpenAPI contract tests</verify>
  <acceptance_criteria>
    - Guest estimate and authenticated project quote use the same pricing service.
    - Expired/changed quotes require a new explicit confirmation.
    - Public payloads contain capability aliases only.
  </acceptance_criteria>
  <done>API contract tests pass.</done>
</task>

</tasks>

<verification>
- [ ] Pricing/hash unit matrix passes.
- [ ] Fresh PostgreSQL concurrency/settlement tests pass.
- [ ] API contract and typecheck pass.
</verification>

<success_criteria>
- No client fallback price remains.
- Duplicate Generate has zero duplicate economic effects.
</success_criteria>

<output>After completion, create `01-02-SUMMARY.md`.</output>

