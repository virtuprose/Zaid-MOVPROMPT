---
phase: 01-production-truth-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/migrations/0013_generation_activation_observability.sql
  - packages/db/migrations/0014_render_processing_stage.sql
  - packages/db/src/service-heartbeat.ts
  - packages/providers/src/runtime-fingerprint.ts
  - apps/api/src/generation-availability.ts
  - apps/api/src/runtime-services.ts
  - apps/worker/src/service-heartbeat.ts
  - apps/worker/src/main.ts
autonomous: true
requirements: [TRUTH-01, TRUTH-02, TRUTH-05, TRUTH-06]
user_setup: []
must_haves:
  truths:
    - Generation is ready only when API, worker, pricing, capabilities, storage, media tools, and quality policy agree.
    - A stale worker or mismatched runtime fingerprint disables only new quotes/submissions.
    - Public readiness never leaks provider identifiers, secrets, signed URLs, or internal diagnostics.
  artifacts:
    - apps/api/src/generation-availability.ts
    - apps/worker/src/service-heartbeat.ts
    - packages/providers/src/runtime-fingerprint.ts
  key_links:
    - Worker heartbeat metadata carries the same fingerprint computed by the API.
    - Feature flags project the availability service rather than environment flags alone.
---

<objective>
Reconcile the production truth tracer from migrations through API and worker startup.

Purpose: prevent the UI from advertising generation when the executable stack cannot finish it.
Output: proven migrations, heartbeat, fingerprint parity, availability reasons, and kill-switch behavior.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-production-truth-foundation/01-CONTEXT.md
@.planning/phases/01-production-truth-foundation/01-RESEARCH.md
@packages/db/src/schema.ts
@apps/api/src/generation-availability.ts
@apps/worker/src/main.ts
</context>

<threat_model>
- Threat: a forged/stale heartbeat enables paid generation. Mitigation: database freshness check plus exact non-secret configuration fingerprint.
- Threat: readiness exposes secrets/provider internals. Mitigation: semantic public reason enum only.
- Threat: API and worker use different buckets/models/policies. Mitigation: shared deterministic fingerprint and fail-closed comparison.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Prove schema and least-privilege runtime state</name>
  <files>packages/db/migrations/0013_generation_activation_observability.sql, packages/db/migrations/0014_render_processing_stage.sql, packages/db/src/service-heartbeat.ts</files>
  <read_first>packages/db/src/schema.ts, packages/db/migrations/0012_pgboss_worker_schema.sql, scripts/infra/check-rls-isolation.sql</read_first>
  <action>Audit and correct migrations/Drizzle schema for service heartbeats, render processing stages, and provider attempt telemetry. Apply all migrations to a fresh PostgreSQL 17 database. Prove the API and worker restricted roles can perform only required operations; do not grant broad database CREATE or bypass RLS.</action>
  <verify>bun run db:check and PostgreSQL migration/RLS integration tests</verify>
  <acceptance_criteria>
    - A clean database reaches migration 0014 with schema and journal matching.
    - A heartbeat can be written by the worker and read for freshness by the API role.
    - Cross-user project/run access remains rejected.
  </acceptance_criteria>
  <done>Fresh migration and role tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile heartbeat and runtime fingerprint</name>
  <files>packages/providers/src/runtime-fingerprint.ts, apps/worker/src/service-heartbeat.ts, apps/worker/src/main.ts</files>
  <read_first>apps/worker/src/config.ts, packages/providers/src/capability-registry.ts, apps/worker/src/pg-boss-worker.ts</read_first>
  <action>Ensure the worker writes a ready heartbeat only after database, queue, capability, storage, media tools, quality reviewer, and output policy initialization succeeds. Heartbeat every configured interval and mark stopping on graceful shutdown. Include only the deterministic fingerprint and boolean readiness metadata; never include secret values.</action>
  <verify>bun test apps/worker/src/service-heartbeat.test.ts apps/worker/src/config.test.ts and bun run --cwd apps/worker typecheck</verify>
  <acceptance_criteria>
    - A fresh heartbeat is emitted after successful startup.
    - A stopped/stale worker is unavailable after 45 seconds by default.
    - Any model, adapter, bucket, output-host, FFmpeg, or quality-policy mismatch changes the fingerprint.
  </acceptance_criteria>
  <done>Worker heartbeat and fingerprint tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify semantic availability and kill switch</name>
  <files>apps/api/src/generation-availability.ts, apps/api/src/runtime-services.ts, apps/api/src/app.ts, packages/contracts/src/api.ts</files>
  <read_first>apps/api/src/generation-pricing.ts, apps/api/src/generation-routes.ts, packages/contracts/src/api.ts</read_first>
  <action>Verify ordered fail-closed availability for disabled, quality, capability, pricing, storage, and worker failures. Ensure the kill switch blocks new quote/start calls while existing run status/output routes stay available. Return only status, reason, and retryable to the browser.</action>
  <verify>bun test apps/api/src/generation-availability.test.ts apps/api/src/app.test.ts apps/api/src/generation.test.ts and bun run --cwd apps/api typecheck</verify>
  <acceptance_criteria>
    - Each dependency removal returns the correct reason.
    - Fingerprint mismatch returns worker_unavailable.
    - Existing accepted runs remain readable when FEATURE_GENERATION is false.
  </acceptance_criteria>
  <done>Availability contract tests and API typecheck pass.</done>
</task>

</tasks>

<verification>
- [ ] Fresh PostgreSQL migrations pass.
- [ ] API and worker focused tests/typechecks pass.
- [ ] Runtime health job produces a fresh heartbeat.
- [ ] Feature flags change correctly when one dependency is removed.
</verification>

<success_criteria>
- All tasks and gates pass without weakening fail-closed behavior.
- No secret/provider diagnostic appears in the public response.
</success_criteria>

<output>After completion, create `01-01-SUMMARY.md`.</output>

