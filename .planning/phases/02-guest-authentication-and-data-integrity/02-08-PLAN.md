---
phase: 02-guest-authentication-and-data-integrity
plan: "08"
type: execute
wave: 8
depends_on: ["02-07"]
files_modified:
  - apps/web/src/features/create/AuthGateDialog.tsx
  - apps/web/src/features/create/AuthGateDialog.test.tsx
  - apps/web/src/features/create/CreatorShell.tsx
  - apps/web/src/pages/Auth.tsx
  - apps/web/src/pages/AuthCallback.tsx
  - apps/web/src/pages/ResetPassword.tsx
  - apps/web/src/pages/AuthCopy.test.ts
  - apps/web/src/features/create/GuestAuthRecovery.test.tsx
  - apps/web/src/i18n/translations/en.ts
  - apps/web/src/i18n/translations/ar.ts
  - scripts/infra/check-phase2-evidence-redaction.mjs
  - .planning/phases/02-guest-authentication-and-data-integrity/02-BROWSER-EVIDENCE.md
  - .planning/phases/02-guest-authentication-and-data-integrity/02-VALIDATION.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, AUTH-08, AUTH-09, AUTH-10, SOURCE-04, SOURCE-05, SOURCE-06, SOURCE-07, PROJ-06]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Generate-time auth, callback, reset, cancellation, claim progress and recovery match the approved English/Arabic UI contract without redesign."
    - "Every form/dialog state is keyboard accessible, focus-correct, responsive, RTL-safe and usable in light/dark themes."
    - "Rendered evidence proves exact recovery, social replay, failure, expiry, wrong-account denial and owner media refresh at all required viewports."
    - "No browser-test dependency is installed; evidence is redacted and failures remain open."
  artifacts:
    - path: "apps/web/src/pages/AuthCopy.test.ts"
      provides: "Concrete complete-string English/Arabic auth and recovery translation contract"
    - path: "apps/web/src/features/create/GuestAuthRecovery.test.tsx"
      provides: "Integrated Generate/auth/callback/cancel/replay/recovery state proof"
    - path: ".planning/phases/02-guest-authentication-and-data-integrity/02-BROWSER-EVIDENCE.md"
      provides: "Rendered responsive, RTL, theme, keyboard and accessibility evidence"
  key_links:
    - from: "apps/web/src/features/create/AuthGateDialog.tsx"
      to: "apps/web/src/config/authProviders.ts"
      via: "Server-derived provider capability"
      pattern: "configuredProviders"
    - from: "apps/web/src/pages/AuthCallback.tsx"
      to: "apps/web/src/features/create/guestClaimRecovery.ts"
      via: "Safe pending-intent recovery and one claim receipt"
      pattern: "pendingGenerationId"
---

<objective>
Finish and prove the approved guest/auth/recovery experience across UI states, browsers, viewports, languages, themes and accessibility conditions.

Purpose: Turn the server/data integrity built in Plans 02-01 through 02-07 into a calm, exact user journey with release-grade evidence.

Output: Approved UI implementation, concrete copy/translation tests, integrated recovery tests and redacted rendered-browser evidence covering all Phase 2 requirements.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-CONTEXT.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-VALIDATION.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-08-01: Complete one Generate → email auth → restored campaign interface</name>
  <files>apps/web/src/features/create/AuthGateDialog.tsx, apps/web/src/features/create/AuthGateDialog.test.tsx, apps/web/src/features/create/CreatorShell.tsx, apps/web/src/pages/AuthCopy.test.ts, apps/web/src/i18n/translations/en.ts</files>
  <read_first>
    - .planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md
    - apps/web/src/features/create/AuthGateDialog.tsx
    - apps/web/src/features/create/CreatorShell.tsx
    - apps/web/src/pages/AuthCopy.test.ts
    - apps/web/src/i18n/translations/en.ts
  </read_first>
  <behavior>
    - Dialog appears only after Generate and shows exactly `Your campaign is ready to create`, the approved preservation description/note and `Continue with email` (or first configured social method).
    - Open traps focus on first enabled method; close/Escape/cancel retains intent/draft and restores focus to `Generate video` with one cancellation announcement.
    - Zero/one/two social methods show exactly the server-configured controls and no blank divider/disabled provider.
    - Claim shell shows factual stages `Creating your private campaign`, `Securing image N of N`, `Checking saved campaign details` with no percentage, render preview, provider or duration.
  </behavior>
  <action>
    Implement the approved UI using the existing creator/auth composition only: warm/near-black semantic surfaces, amber primary/focus token, Inter/Inter Tight/Noto Arabic, four-pixel scale, 44px controls, 160ms restrained transition and reduced-motion override. Use native labelled controls, Dialog semantics, localized close name, `role=status` polite atomic claim stages and no duplicate toast. Build `AuthCopy.test.ts` as a concrete complete-string contract that imports translation catalogs and asserts every key from the UI-SPEC English/Arabic table exists as a whole string; include preservation title/description/note, email actions, reset neutral copy, save/cancel/loading/claim/success/failure/offline/mismatch/expiry/replay/reminder/source failure and rights confirmation. Do not redesign or add tokens/components.
  </action>
  <acceptance_criteria>
    - Generate is the first auth prompt; dialog focus/open/close behavior and exact English copy pass Testing Library.
    - Provider zero/one/two and non-blocking verification reminder states match server capability/policy.
    - AuthCopy test fails on any missing, concatenated or changed English/Arabic required string.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web test -- AuthGateDialog AuthCopy CreatorShell &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>One complete email Generate/auth/claim interface follows the approved design and exact copy contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-08-02: Complete callback, reset, replay and recovery states in both languages</name>
  <files>apps/web/src/pages/Auth.tsx, apps/web/src/pages/AuthCallback.tsx, apps/web/src/pages/ResetPassword.tsx, apps/web/src/features/create/GuestAuthRecovery.test.tsx, apps/web/src/i18n/translations/ar.ts</files>
  <read_first>
    - .planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md
    - apps/web/src/pages/Auth.tsx
    - apps/web/src/pages/AuthCallback.tsx
    - apps/web/src/pages/ResetPassword.tsx
    - apps/web/src/lib/auth/returnPath.ts
    - apps/web/src/i18n/translations/ar.ts
  </read_first>
  <behavior>
    - Auth/callback/reset preserve field values, focus h1/first invalid field, use safe return priority and neutral reset account-existence copy.
    - Callback replay uses navigation replace, opens the existing project and emits at most one polite `Your campaign is already secured. Opening it now.` announcement.
    - Claim failure/offline/session mismatch/expiry show the exact complete English/Arabic UI-SPEC copy/actions and preserve local state where defined.
    - Unverified user sees the quiet reminder and remains able to finish the first campaign.
  </behavior>
  <action>
    Connect `/auth`, `/auth/callback` and reset to the safe contract from Plan 02-03 and recovery state machine from Plans 02-02/02-04. Use the approved exact strings, `aria-describedby`, `aria-invalid`, first-error focus, one polite loading/status region, `role=alert` failures and logical RTL layout. For claim failure focus `Retry securing image`; for cancellation return to unchanged creator; offline enables explicit retry after reconnect but never auto-claims; mismatch reveals no other account; expired draft exposes only `Start a new campaign`. Add integrated tests for email, deterministic Google/Apple callback fixtures, replay/second tab, cancel, reset, offline, claim retry, source replacement, mismatch and expiry in English/Arabic/light/dark state classes.
  </action>
  <acceptance_criteria>
    - Every approved auth/claim/recovery state has exact bilingual copy, correct action, focus and announcement behavior.
    - Campaign and local media remain unchanged on cancel/failure/offline/mismatch.
    - Replay opens one existing project without duplicate animation/toast/claim.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web test -- AuthCopy GuestAuthRecovery returnPath &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>The full approved recovery matrix is implemented accessibly in English and Arabic.</done>
</task>

<task type="auto">
  <name>Task 02-08-03: Capture redacted rendered-browser evidence and close the phase gate</name>
  <files>scripts/infra/check-phase2-evidence-redaction.mjs, .planning/phases/02-guest-authentication-and-data-integrity/02-BROWSER-EVIDENCE.md, .planning/phases/02-guest-authentication-and-data-integrity/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md
    - .planning/phases/02-guest-authentication-and-data-integrity/02-VALIDATION.md
    - .planning/phases/02-guest-authentication-and-data-integrity/02-CONTEXT.md
    - .planning/phases/01-production-truth-foundation/01-VERIFICATION.md
  </read_first>
  <action>
    Keep `@playwright/test` absent. Use the available in-app browser or Chrome control with the existing local PostgreSQL 17/private-storage/API/worker/web stack. Exercise a guest campaign with two images through email, Google stub and Apple stub flows; cancel once, replay callback/tabs, fail/retry one asset, go offline, test expiry/wrong account, replace source, refresh an expired owner URL and verify dashboard ownership. Capture 375/768/1024/1440, English LTR/Arabic RTL, light/dark, keyboard-only, focus trap/restore, reduced motion, scroll width, accessibility tree/Axe, console/network and request/database/object-count evidence. Record only safe request IDs, counts and checksum agreement. Add a quick evidence redaction script that fails on passwords/tokens/cookies/OAuth state, signed URL query patterns, raw remote URLs, private object keys, secrets or other-user identifiers. Update each validation row with evidence; do not mark green on an observed failure.
  </action>
  <acceptance_criteria>
    - Rendered matrix proves exact recovery, private claim, social replay, cancellation, retry, expiry, source invalidation, wrong-account denial and owner URL refresh at all required conditions.
    - Keyboard, dialog semantics, focus, 44px targets, contrast, reduced motion, RTL order, overflow and console checks pass or remain explicitly open.
    - Evidence redaction and web lint pass; no new browser dependency appears in package files.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web lint &amp;&amp; node scripts/infra/check-phase2-evidence-redaction.mjs &amp;&amp; ! rg -n '"@playwright/test"' package.json bun.lock</automated>
    <human-check>Independently complete Generate → auth → restored campaign at 375px English/light and Arabic/dark. Cancel, retry a failed image, replay the callback and verify a second user cannot see the first user’s project/media. Compare the results with `02-BROWSER-EVIDENCE.md`.</human-check>
  </verify>
  <done>Phase 2 has redacted rendered proof for the complete user journey and every failure remains visible until resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Auth UI/callback → IndexedDB claim | Navigation, session and local intent can replay or lose data. |
| Rendered evidence → repository | Screenshots/network records can leak sensitive material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-35 | Spoofing | Callback/session UI | high | mitigate | Safe callback contract and wrong-account rendered proof. |
| T-02-36 | Tampering | Recovery state | high | mitigate | Typed state machine and exact receipt before cleanup/navigation. |
| T-02-37 | Repudiation | Replay announcements | medium | mitigate | One project/receipt and one polite status assertion. |
| T-02-38 | Information disclosure | Browser evidence | high | mitigate | Automated redaction plus safe evidence fields only. |
| T-02-39 | Denial of service | Offline/retry loop | medium | mitigate | Explicit retry only; no reconnect auto-submit. |
| T-02-SC | Tampering | Browser package install | high | mitigate | No install; any future dependency requires a separate blocking human provenance/version checkpoint before package modification. |
</threat_model>

## Source Coverage Audit

| Source | Items | Plans | Status |
|---|---|---|---|
| GOAL | Configure as guest → auth at Generate → exact owned recovery with private media | 02-01..02-08 | COVERED |
| REQ | AUTH-01, AUTH-02, AUTH-06, AUTH-09 | 02-02, 02-08 | COVERED |
| REQ | AUTH-03, AUTH-04, AUTH-05 | 02-03, 02-08 | COVERED |
| REQ | AUTH-07, AUTH-08, AUTH-10 | 02-01, 02-02, 02-03, 02-04, 02-06, 02-07, 02-08 | COVERED |
| REQ | SOURCE-04 | 02-05, 02-08 | COVERED |
| REQ | SOURCE-05 | 02-04, 02-05, 02-07, 02-08 | COVERED |
| REQ | SOURCE-06, SOURCE-07 | 02-04, 02-06, 02-08 | COVERED |
| REQ | PROJ-06 | 02-04, 02-07, 02-08 | COVERED |
| RESEARCH | Durable claim/service/repository, canonical cleanup, configured auth policy/providers | 02-01..02-03 | COVERED |
| RESEARCH | Private integrity, PostgreSQL quotas/shared SSRF, source truth | 02-04..02-06 | COVERED |
| RESEARCH | 24h audited cleanup, restricted RLS/migration proof, rendered UI | 02-07, 02-08 | COVERED |
| CONTEXT | Template/Advanced shared ownership; auth only at Generate; 7-day same-browser; no guest cloud data | 02-01, 02-02, 02-08 | COVERED |
| CONTEXT | Deferred verification; configured social only; safe returns; pending intent priority/replay | 02-03, 02-08 | COVERED |
| CONTEXT | Exact field/media preservation; verified cleanup; PostgreSQL/private storage authority; response-only URLs | 02-01, 02-02, 02-04, 02-07 | COVERED |
| CONTEXT | Cross-account privacy; shared scan/mirror security; approved responsive/RTL/a11y UI | 02-05, 02-07, 02-08 | COVERED |
| CONTEXT | Deferred cross-device drafts, mandatory first-campaign verification, teams, billing, Digital Twins | — | EXCLUDED (deferred) |

## Artifacts this phase produces

- Exact Generate-time auth/callback/reset/claim/recovery UI.
- Concrete full-string `AuthCopy.test.ts` translation contract.
- Integrated English/Arabic state tests.
- Redacted rendered-browser evidence at four viewports, two languages and two themes.
- Updated Phase 2 validation sign-off.

<verification>
- Task verifies remain focused; evidence task runs only web lint and redaction.
- Phase gate only: run `bun run test:all &amp;&amp; bun run typecheck &amp;&amp; bun run db:check`, the guarded PostgreSQL 17 migration/RLS harness, worker cleanup tests and rendered matrix before setting `nyquist_compliant: true`.
- Keep all failures open and never infer visual/security success from code presence.
</verification>

<success_criteria>
- All fifteen Phase 2 requirements have automated plus rendered/service evidence.
- Exact guest recovery works without duplication, cross-user exposure or premature local deletion.
- Approved UI passes responsive, RTL, theme, keyboard and WCAG 2.2 AA checks without redesign.
</success_criteria>

<output>
Create `.planning/phases/02-guest-authentication-and-data-integrity/02-08-SUMMARY.md` when done.
</output>
