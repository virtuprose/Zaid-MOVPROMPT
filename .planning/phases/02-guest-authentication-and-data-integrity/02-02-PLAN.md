---
phase: 02-guest-authentication-and-data-integrity
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/features/create/guestDraftStore.ts
  - apps/web/src/features/create/guestDraftStore.test.ts
  - apps/web/src/features/create/guestClaimRecovery.ts
  - apps/web/src/features/create/guestClaimRecovery.test.ts
  - apps/web/src/features/create/CreateStudio.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-06, AUTH-07, AUTH-09]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A guest can configure the complete campaign and local blobs for seven days without creating cloud data."
    - "Generate persists one stable pendingGenerationId before authentication and cancel returns to the unchanged campaign."
    - "Local JSON and blobs are removed only after canonical server configuration and every asset checksum match."
  artifacts:
    - path: "apps/web/src/features/create/guestDraftStore.ts"
      provides: "Seven-day IndexedDB JSON/blob/pending-intent lifecycle"
    - path: "apps/web/src/features/create/guestClaimRecovery.ts"
      provides: "Exact receipt comparison, resumable claim state and verified cleanup"
  key_links:
    - from: "apps/web/src/features/create/CreateStudio.tsx"
      to: "apps/web/src/features/create/guestDraftStore.ts"
      via: "Persist complete draft and intent before opening auth"
      pattern: "pendingGenerationId"
    - from: "apps/web/src/features/create/guestClaimRecovery.ts"
      to: "apps/web/src/features/create/guestDraftStore.ts"
      via: "Delete only after configuration and asset receipt verification"
      pattern: "deleteGuestDraft"
---

<objective>
Make the browser-side guest draft exact, seven-day durable and safe across Generate, cancellation, reload and claim recovery.

Purpose: Preserve the user’s complete work locally until the authenticated claim boundary from Plan 02-01 returns a verified canonical receipt.

Output: IndexedDB lifecycle, stable pending intent, exact comparison/cleanup helpers and focused browser-storage tests for AUTH-01, AUTH-02, AUTH-06, AUTH-07 and AUTH-09.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-CONTEXT.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-02-01: Persist one complete guest campaign and stable Generate intent</name>
  <files>apps/web/src/features/create/guestDraftStore.ts, apps/web/src/features/create/guestDraftStore.test.ts, apps/web/src/features/create/guestClaimRecovery.ts, apps/web/src/features/create/CreateStudio.tsx</files>
  <read_first>
    - apps/web/src/features/create/guestDraftStore.ts
    - apps/web/src/features/create/CreateStudio.tsx
    - apps/web/src/features/create/portableProjectMapper.ts
    - packages/contracts/src/guest-claims.ts
    - .planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md
  </read_first>
  <behavior>
    - A guest saves one Template or Advanced campaign with all fields, rights, references and local blobs and reloads the exact values in the same browser.
    - A page visit or ordinary edit causes zero API project/asset creation and no anonymous cloud upload.
    - Selecting Generate validates the campaign, persists one stable pendingGenerationId/idempotency intent, then opens the existing auth boundary.
    - Closing/cancelling auth keeps the same intent, JSON and blobs and restores focus to Generate.
  </behavior>
  <action>
    Start with failing IndexedDB tests. Normalize the existing draft store into one versioned record containing exact campaign JSON, opaque local blob keys, creation/update/expiry timestamps, rights state and optional stable pending intent. Store blobs separately but transactionally associate them with the draft. In `CreateStudio`, persist the full current snapshot and stable pendingGenerationId before any auth navigation/dialog; do not create a project, call asset upload, or clear a field during this step. `guestClaimRecovery` loads only the same-browser record and prepares the Plan 02-01 contract without introducing account identifiers. Wire cancel/Escape back to the unchanged editor; visual copy is completed in Plan 02-08.
  </action>
  <acceptance_criteria>
    - Reload restores every campaign field/reference/blob key exactly and Generate reuses one pending intent.
    - No guest page visit, save or auth cancel creates a cloud project, object or anonymous account.
    - Cancellation keeps the exact snapshot and Generate focus target available.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web test -- guestDraftStore guestClaimRecovery &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>A complete guest campaign survives reload and Generate-time auth interruption without cloud state or data loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-02-02: Enforce seven-day expiry and verified canonical cleanup</name>
  <files>apps/web/src/features/create/guestDraftStore.ts, apps/web/src/features/create/guestDraftStore.test.ts, apps/web/src/features/create/guestClaimRecovery.ts, apps/web/src/features/create/guestClaimRecovery.test.ts</files>
  <read_first>
    - apps/web/src/features/create/guestDraftStore.ts
    - apps/web/src/features/create/guestClaimRecovery.ts
    - packages/contracts/src/guest-claims.ts
    - .planning/phases/02-guest-authentication-and-data-integrity/02-CONTEXT.md
  </read_first>
  <behavior>
    - Non-expired JSON and blobs remain recoverable for seven days; expiry removes the JSON/blob set as one guarded local operation and exposes no partial campaign.
    - Claim failure, offline state, checksum mismatch, configuration mismatch or wrong account retains the complete local draft and pending intent.
    - Exact canonical configuration plus every expected asset ID/checksum marks local cleanup eligible and deletion executes once.
    - Replaying a successful receipt after cleanup is harmless and cannot reconstruct or cross-claim another user’s local draft.
  </behavior>
  <action>
    Add a deterministic clock seam and explicit `load`, `expire`, `markClaimCheckpoint`, `verifyCanonicalReceipt` and `deleteVerifiedDraft` operations. Canonical comparison must cover every locked campaign field and an ordered asset manifest; unknown/missing fields fail closed. Cleanup runs only after the Plan 02-01 receipt and every server asset digest match, then deletes JSON and all referenced blobs atomically/idempotently. Expiry uses the stored creation/expiry values, never server/session identity. Keep failed/mismatched data intact and return typed recovery states for the later UI; never auto-submit on reconnect.
  </action>
  <acceptance_criteria>
    - Tests cover day 6/day 7 boundary, partial blob set, missing receipt field, checksum mismatch, failure/retry, wrong-account marker and cleanup replay.
    - Any verification failure leaves JSON, blobs and intent unchanged.
    - Verified cleanup removes only the matching draft/blob set once.
  </acceptance_criteria>
  <verify>
    <automated>bun run --cwd apps/web test -- guestDraftStore guestClaimRecovery &amp;&amp; bun run --cwd apps/web typecheck</automated>
  </verify>
  <done>Local draft retention and destruction are exact, testable and independent of auth navigation timing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Creator state → IndexedDB | Untrusted mutable form state and blobs become a recoverable local record. |
| IndexedDB receipt comparison → deletion | Server response controls irreversible local cleanup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-06 | Tampering | Local snapshot | high | mitigate | Versioned schema, exact digest/manifest comparison and fail-closed unknown fields. |
| T-02-07 | Repudiation | Pending intent reuse | medium | mitigate | Persist before auth and retain through cancel/reload. |
| T-02-08 | Information disclosure | Expired/other-account draft | high | mitigate | Same-browser local keys, generic typed mismatch state and no account data. |
| T-02-09 | Denial of service | Partial cleanup | high | mitigate | Transactional associated blob deletion only after verified receipt. |
| T-02-SC | Tampering | Package installation | high | mitigate | Existing IndexedDB/test dependencies only; no install. |
</threat_model>

## Artifacts this phase produces

- Versioned seven-day guest campaign/blob record.
- Stable Generate-time pending intent.
- Exact canonical receipt verifier.
- Failure-preserving and idempotent local cleanup tests.

<verification>
- Run both focused web test files and web typecheck.
- Inspect tests for zero cloud calls before authentication and exact field/manifest comparison.
</verification>

<success_criteria>
- Guests configure and reload the complete campaign locally for seven days.
- Generate is the first auth boundary and cancellation changes nothing.
- Local data disappears only after verified canonical ownership transfer.
</success_criteria>

<output>
Create `.planning/phases/02-guest-authentication-and-data-integrity/02-02-SUMMARY.md` when done.
</output>
