---
phase: 04-durable-generation-and-accepted-quality
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - packages/db/src/generation-service.ts
  - packages/db/test/generation-service.postgres.test.ts
  - apps/worker/src/render-lifecycle.ts
  - apps/worker/src/render-lifecycle.test.ts
  - docs/runbooks/PHASE4_PAID_CANARY.md
  - apps/api/src/creator-routes.ts
  - apps/api/src/generation.test.ts
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/GoldenPathStates.test.tsx
  - scripts/verify-phase4-redaction.ts
  - .planning/phases/04-durable-generation-and-accepted-quality/04-CANARY-EVIDENCE.md
autonomous: false
requirements: [GEN-05, GEN-06, GEN-07, GEN-08, GEN-09, GEN-10, GEN-11, GEN-12, GEN-13, GEN-14]
user_setup:
  - service: Vercel AI Gateway production-model canary
    why: One real approved Seedance 2.5 operation is required to prove the live asynchronous contract, output host, private media acceptance, cost, recovery, and settlement.
    env_vars:
      - name: AI_GATEWAY_API_KEY
        source: Vercel team AI Gateway server-side secret; rotate any key previously exposed in chat or logs.
      - name: MOVPROMPT_GATEWAY_CONFIRM_PAID_VIDEO
        source: Set to YES only for the single authorized canary command in the runbook.
    dashboard_config:
      - task: Record an explicit current USD budget cap and owner; keep automatic balance reload disabled.
        location: Vercel AI Gateway billing/budget settings and the Phase 04 checkpoint response.
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/04-durable-generation-and-accepted-quality/04-02-SUMMARY.md
  - .planning/phases/04-durable-generation-and-accepted-quality/04-CONTEXT.md
  - .planning/phases/04-durable-generation-and-accepted-quality/04-AI-SPEC.md
  - .planning/phases/04-durable-generation-and-accepted-quality/04-VALIDATION.md
  - .planning/phases/04-durable-generation-and-accepted-quality/04-COVERAGE.md
  - apps/worker/src/__fixtures__/kw-video-48-v1.calibration.json
  - docs/runbooks/GENERATION_ACTIVATION.md
  - docs/product/VERCEL_GATEWAY_LOCAL.md
must_haves:
  truths:
    - Provider acceptance, internal retries, concurrent reconciliation, terminal failure, and cancellation produce one exact entitlement/credit outcome (D-14, D-17, D-19).
    - A failed or cancelled newer working version cannot replace or remove the previous accepted version/output (D-11, D-16, D-21).
    - Only the authenticated owner can request a fresh short-lived URL for an accepted private output; URL expiry never destroys the saved project (D-08).
    - Public status/download/log evidence contains no provider ID, operation ID, raw error, secret, stable object key, signed/provider URL persistence, reviewer evidence, or internal retry details (D-20, D-22).
    - Live submission and acceptance remain blocked until Task 04-02-03 separately approves genuine two-reviewer human-attested 48-case calibration evidence bound to candidate checksums and the exact evaluator/rubric pair; the canary never substitutes for that checkpoint (D-13, D-24, D-25).
    - Phase completion is blocked until one explicitly budget-authorized Seedance 2.5 operation survives browser/worker interruption and proves private accepted media plus exact settlement (D-24, D-25, D-26).
  artifacts:
    - path: packages/db/test/generation-service.postgres.test.ts
      provides: Disposable-PostgreSQL concurrent settlement, cancellation, retry, restart, and accepted-pointer evidence
    - path: apps/api/src/creator-routes.ts
      provides: Owner-authorized no-store accepted-output download refresh
    - path: docs/runbooks/PHASE4_PAID_CANARY.md
      provides: Stop-safe one-operation canary and same-operation recovery instructions
    - path: .planning/phases/04-durable-generation-and-accepted-quality/04-CANARY-EVIDENCE.md
      provides: Sanitized budget, operation, host, cost, timing, checksum, media, quality, restart, and ledger evidence
  key_links:
    - from: apps/worker/src/render-lifecycle.ts
      to: packages/db/src/generation-service.ts
      via: Row-locked acceptance, release, and refund methods with deterministic ledger keys
      pattern: finalizeProviderAccepted|releaseRenderReservation|refundRender
    - from: packages/db/src/generation-service.ts
      to: creator project accepted-version pointer
      via: Final approved completion updates accepted state in the same owner-scoped terminal transaction
      pattern: acceptedVersion|completed|for\("update"\)
    - from: apps/api/src/creator-routes.ts
      to: private output storage signer
      via: Owner lookup followed by a new short-lived no-store download response
      pattern: findOwnedOutput|signDownload|noStore
    - from: docs/runbooks/PHASE4_PAID_CANARY.md
      to: .planning/phases/04-durable-generation-and-accepted-quality/04-CANARY-EVIDENCE.md
      via: One authorized production-model operation with redacted evidence fields
      pattern: authorization|provider operation|checksum|ledger
    - from: apps/worker/src/__fixtures__/kw-video-48-v1.calibration.json
      to: docs/runbooks/PHASE4_PAID_CANARY.md
      via: Pre-submission and pre-acceptance gate verifies the separately approved human checkpoint, candidate checksums, reviewer-role attestations, adjudication, approval record, exact evaluator/rubric versions, kappa/Spearman thresholds, zero critical false accepts, and all 48 labeled cases
      pattern: candidateChecksum|reviewerRoleAttestations|approvalRecord|evaluatorVersion|rubricVersion|weightedKappa|spearman|criticalFalseAccepts
---

<objective>
**As a** MovPrompt customer, **I want to** keep my last successful video and receive exactly one fair settlement through failures, cancellation, and expired links, **so that** generation is financially and operationally trustworthy.

Prove exact terminal settlement, previous-success preservation, owner-only output refresh, complete public redaction, and the one authorized live production-model acceptance path per D-08, D-11, and D-14 through D-26.

Purpose: Close every concurrency, cancellation, recovery, public-boundary, and real-evidence gap before Phase 04 can become authoritative.
Output: PostgreSQL race proof, fresh-download/browser proof, redaction gate, paid-canary runbook, and one explicitly approved sanitized canary evidence record.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-CONTEXT.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-AI-SPEC.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-VALIDATION.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-COVERAGE.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-02-SUMMARY.md

<interfaces>
Preserve `GenerationService` row-lock/idempotency contracts, append-only ledger keys, starter entitlement states, working/accepted project-version separation, owner-scoped output lookup, response-time signing, and `private, no-store` output responses. Existing migrations 0008/0013/0017 already carry attempt telemetry and forced RLS; this plan does not add schema. Apply all repository migrations to disposable PostgreSQL 17 before the integration race suite, and never use destructive schema push.
</interfaces>

<execution_discipline>
Commit each task independently with only its listed files and evidence. Task 04-03-01 owns database/worker/runbook work; Task 04-03-02 owns API/frontend/redaction work. Never mix frontend and worker changes in one task commit, and never stage unrelated dirty-tree files.
</execution_discipline>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 04-03-01: Settle one run exactly once under concurrent terminal paths</name>
  <precondition>Either Docker can provision the exact isolated PostgreSQL 17 container documented below, or the CI job supplies the equivalent disposable PostgreSQL 17 service URL as `MOVPROMPT_TEST_DATABASE_URL`; a shared development/staging/production database is forbidden.</precondition>
  <read_first>packages/db/src/generation-service.ts, packages/db/test/generation-service.postgres.test.ts, apps/worker/src/render-lifecycle.ts, apps/worker/src/render-lifecycle.test.ts, packages/db/migrations/0008_render_quality_attempts.sql, packages/db/migrations/0013_generation_activation_observability.sql</read_first>
  <files>packages/db/src/generation-service.ts, packages/db/test/generation-service.postgres.test.ts, apps/worker/src/render-lifecycle.ts, apps/worker/src/render-lifecycle.test.ts, docs/runbooks/PHASE4_PAID_CANARY.md</files>
  <behavior>
    - Concurrent provider acceptance, duplicate job delivery, quality retry, cancellation, failure, timeout, and terminal reconciliation retain one provider operation and one charge/entitlement transition (D-19).
    - A provider/technical/quality failure after acceptance refunds/restores once; a failure before acceptance releases once; repeated terminal calls are idempotent (D-16, D-17, D-19).
    - Pre-acceptance cancellation releases immediately; post-acceptance cancellation cannot refund until confirmed provider cancellation/failure (D-17, D-18).
    - Attempt ordinals and telemetry remain immutable/sanitized; an internal retry has no new customer charge (D-14, D-20).
    - Accepted pointer advances only after final approval; failed newer work retains the prior accepted pointer/object (D-11, D-21).
  </behavior>
  <action>Add disposable-PostgreSQL race/restart tests before tightening transaction boundaries. Race two worker instances plus cancel/retry/terminal calls against the same run, using barriers and independent database connections rather than sequential mocks. Assert one provider request, one attempt per ordinal, one deterministic charge key, one deterministic refund/release key, correct reservation/entitlement state, and unchanged previous accepted pointer on failure. Keep `FOR UPDATE`, owner scoping, unique constraints, and append-only ledger transitions as the authority. In `docs/runbooks/PHASE4_PAID_CANARY.md`, document this exact local isolation contract: assert the name `movprompt-phase4-pg17` is unused; install `trap 'docker stop movprompt-phase4-pg17 &gt;/dev/null 2&gt;&amp;1 || true' EXIT`; run `docker run --detach --rm --name movprompt-phase4-pg17 --publish 127.0.0.1:55434:5432 --env POSTGRES_USER=movprompt_phase4 --env POSTGRES_PASSWORD=movprompt_phase4 --env POSTGRES_DB=movprompt_phase4 postgres:17-alpine`; wait with `until docker exec movprompt-phase4-pg17 pg_isready -U movprompt_phase4 -d movprompt_phase4; do sleep 1; done`; export `MOVPROMPT_TEST_DATABASE_URL=postgresql://movprompt_phase4:movprompt_phase4@127.0.0.1:55434/movprompt_phase4`; apply `DATABASE_URL_DIRECT="$MOVPROMPT_TEST_DATABASE_URL" bun run db:migrate`; run `DATABASE_URL="$MOVPROMPT_TEST_DATABASE_URL" DATABASE_URL_DIRECT="$MOVPROMPT_TEST_DATABASE_URL" bun run db:check`, the PostgreSQL race test, and worker lifecycle test; then run `docker stop movprompt-phase4-pg17` and confirm the named container is absent. Also document the repository CI equivalent as an ephemeral job-scoped `postgres:17-alpine` service with the same user/password/database, health command, migration/test commands, and automatic runner teardown. Abort if the URL host/database does not match the disposable contract; never mutate a shared database. Add the separate human-calibration preflight, kill-switch, browser/worker restart, unknown-host same-operation recovery, private checksum/media/quality checks, cancellation matrix, settlement queries, sanitization, and abort steps. State that the paid canary cannot create, replace, or waive calibration evidence. Do not run it in this task, and make one database/worker/runbook-only commit.</action>
  <verify><automated>test -n "$MOVPROMPT_TEST_DATABASE_URL" &amp;&amp; DATABASE_URL_DIRECT="$MOVPROMPT_TEST_DATABASE_URL" bun run db:migrate &amp;&amp; DATABASE_URL="$MOVPROMPT_TEST_DATABASE_URL" DATABASE_URL_DIRECT="$MOVPROMPT_TEST_DATABASE_URL" bun run db:check &amp;&amp; MOVPROMPT_TEST_DATABASE_URL="$MOVPROMPT_TEST_DATABASE_URL" bun run --cwd packages/db test --run test/generation-service.postgres.test.ts &amp;&amp; bun run --cwd apps/worker test --run src/render-lifecycle.test.ts &amp;&amp; test -s docs/runbooks/PHASE4_PAID_CANARY.md &amp;&amp; rg -q "movprompt-phase4-pg17" docs/runbooks/PHASE4_PAID_CANARY.md &amp;&amp; rg -q "postgres:17-alpine" docs/runbooks/PHASE4_PAID_CANARY.md &amp;&amp; rg -q "docker stop" docs/runbooks/PHASE4_PAID_CANARY.md &amp;&amp; rg -q "cannot create, replace, or waive" docs/runbooks/PHASE4_PAID_CANARY.md</automated></verify>
  <acceptance_criteria>
    - Disposable PostgreSQL 17 applies all repository migrations and passes schema/RLS checks before the race suite.
    - The runbook gives exact isolated local provision, readiness, migration, test, teardown, absence check, and equivalent ephemeral CI service instructions; it refuses shared database URLs.
    - Every concurrent scenario records one provider operation and exactly one economic terminal outcome.
    - At most three total attempts exist, and quality retries create no additional customer charge.
    - A failed/cancelled newer version leaves the previous accepted version/object reference unchanged.
    - The canary runbook contains no secret values and cannot run without the explicit confirmation guard.
    - The canary runbook blocks submission and acceptance on valid exact-version calibration and explicitly rejects canary-as-calibration substitution.
  </acceptance_criteria>
  <done>Database and worker evidence prove exactly-once settlement and last-success preservation under real concurrent terminal races.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-03-02: Refresh accepted output securely and prove the public boundary</name>
  <read_first>apps/api/src/creator-routes.ts, apps/api/src/generation.test.ts, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/GoldenPathStates.test.tsx, scripts/verify-phase4-redaction.ts, AGENTS.md</read_first>
  <files>apps/api/src/creator-routes.ts, apps/api/src/generation.test.ts, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/GoldenPathStates.test.tsx, scripts/verify-phase4-redaction.ts</files>
  <behavior>
    - An authenticated owner can request a new short-lived no-store download URL for an accepted private output after the previous URL expires (D-08, GEN-13).
    - Another user, another project, non-accepted run, wrong bucket/key relationship, missing object, and expired session receive safe denial without signing (GEN-13, GEN-14).
    - Database/project/status responses persist stable output metadata only; provider/signed URLs are response-local and cannot survive reload/cache persistence (D-08, D-20).
    - The browser preserves the last accepted playback/download through a failed regeneration, explains pending cancellation honestly, and refreshes output access on demand (D-11, D-17, D-22).
    - Status, download, error, evidence, and structured-log payloads pass the forbidden-field verifier (D-20, GEN-14).
  </behavior>
  <action>Write owner/cross-owner/expired-link/non-accepted output route tests and browser reload states first. Authorize the stable bucket/key relationship through the repository and sign only after owner/project/run acceptance checks; set `private, no-store` and issue a new URL for every authorized refresh. Do not store the returned URL in project state, IndexedDB, localStorage, logs, analytics, or database. Before any CreateStudio/frontend change, invoke and follow the required skills in this exact order: `ux-research-ia` → `visual-art-direction` → `design-system-token-architect` → `interaction-state-microcopy` → `accessibility-visual-qa`; record how their guidance affected the bounded state/copy change and retain the existing visual language. Then verify saved-project, pending-cancel, previous-success, ready playback/download, expired refresh, keyboard/focus/live-status, RTL, and light/dark behavior. Expand the structured redaction verifier across API fixtures, browser cache snapshots, evidence templates, and logs.</action>
  <verify>
    <automated>bun run --cwd apps/api test --run src/generation.test.ts &amp;&amp; bun run --cwd apps/web test --run src/features/create/GoldenPathStates.test.tsx &amp;&amp; bun scripts/verify-phase4-redaction.ts &amp;&amp; bun run eval:phase04</automated>
    <human-check>With a provisioned non-provider stack, inspect accepted playback/download refresh and pending cancellation at 375, 768, 1024, and 1440 in light/dark and English/Arabic RTL; verify keyboard, focus, live announcements, no overflow, clean console, and no persisted signed URL.</human-check>
  </verify>
  <acceptance_criteria>
    - Owner refresh succeeds after URL expiry; every cross-user/project/non-accepted/missing-object case fails without invoking the signer.
    - Repeated refresh returns response-local short-lived access while saved project/run records remain URL-free.
    - Failed regeneration retains visible/playable last accepted output and a clear retry path.
    - Pending cancellation copy never implies provider cancellation or refund is complete.
    - Redaction, responsive, locale, theme, keyboard, focus, live-region, overflow, and console evidence pass.
    - Required UX skills were applied in the mandated order before the final full non-billable `eval:phase04` execution.
  </acceptance_criteria>
  <done>The owner can always refresh a valid accepted output safely, and public/browser evidence remains truthful and fully redacted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 04-03-03: Authorize and verify one real Seedance 2.5 acceptance canary</name>
  <what-built>All non-billable provider, worker, storage, media, settlement, cancellation, output-refresh, browser, and redaction gates; the separately approved qualified-human 48-case calibration from Task 04-02-03; and `docs/runbooks/PHASE4_PAID_CANARY.md`.</what-built>
  <how-to-verify>
    1. Confirm blocking Task 04-02-03 was separately approved after two independently completed qualified-human labels per candidate, adjudication, reviewer-role attestations, candidate checksums, evaluator/rubric versions, approval record, weighted kappa >= 0.70, every applicable per-dimension Spearman >= 0.70, and zero critical false accepts. Re-run the validator against that approved artifact. Missing/stale/failed/unapproved evidence blocks submission and acceptance; this canary cannot produce labels, replace the human checkpoint, or waive it.
    2. Provide an explicit current USD budget cap and authorize exactly one `bytedance/seedance-2.5` canary. A prior authorization or account balance is not sufficient (D-24, D-25).
    3. After authorization, the executor follows `docs/runbooks/PHASE4_PAID_CANARY.md`; it submits once with one stable idempotency key, closes the browser, stops/restarts the worker, and proves status resumes the persisted operation.
    4. If the returned hostname is not already exact-reviewed, stop acquisition, record hostname only, review/update configuration, and reconcile the same operation. Do not submit another generation.
    5. Before acceptance, revalidate that the evaluator/rubric versions on the candidate exactly match the passing calibration artifact; then verify the privately stored final MP4 checksum, MP4/H.264/AAC policy, quoted ratio/resolution/duration/audio, full decode, complete quality decision, accepted pointer, provider cost/latency/usage, and exactly one charge/entitlement outcome.
    6. Run the provider failure, invalid output, quality exhaustion, pre-acceptance cancellation, and post-acceptance pending-cancellation settlement fixtures; the real canary need not be deliberately destroyed to prove fixture-only failure cases.
    7. Record only sanitized evidence in `04-CANARY-EVIDENCE.md`: authorization/cap/owner/date, configuration hash, internal operation correlation hash (not the raw ID), hostname, timings, micro-USD cost, private object checksum/media report, evaluator/rubric decision, calibration artifact ID/pass result, restart timeline, ledger/entitlement keys/outcomes, request IDs, and redaction result.
    8. After all canary evidence is recorded, run `bun run eval:phase04`, the PostgreSQL race suite, full `test:all`, typecheck, build, redaction verifier, and the rendered browser matrix. Any missing row, duplicate transition, unsafe field, unknown unresolved host, failed calibration, or failed quality/media check keeps Phase 04 incomplete.
  </how-to-verify>
  <resume-signal>First reply `Authorize one Seedance 2.5 canary up to USD <cap>` to permit the executor's single paid call. After the executor returns the redacted evidence, reply `approved` or describe the failed evidence row. Choosing to defer authorization leaves Phase 04 correctly incomplete.</resume-signal>
  <done>One currently authorized production-model operation proves D-25/D-26 end to end only after valid exact-version 48-case calibration, and every non-billable failure/cancellation/security gate remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Concurrent workers/API to PostgreSQL | Acceptance, retry, cancellation, and refund calls race on user value and accepted pointers. |
| Owner browser to private output signer | Session/project/run identity controls short-lived access to accepted media. |
| Live canary to Gateway/storage/quality/ledger | One billable external operation crosses every production trust boundary. |
| Operational evidence to repository/client | Evidence can leak credentials, provider operations, signed URLs, object keys, or customer media. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-11 | Tampering / Repudiation | Concurrent settlement | critical | mitigate | Disposable-PostgreSQL barriers, row locks, deterministic ledger keys, unique operation/attempt constraints, exact balance/entitlement assertions. |
| T-04-12 | Tampering | Accepted-version pointer | critical | mitigate | Advance only in final approved transaction; regression/race tests retain prior accepted pointer/object on newer failure. |
| T-04-13 | Information disclosure / Elevation | Download refresh | critical | mitigate | Owner/project/run/accepted-object lookup before short-lived signing; cross-user/project tests; private no-store response. |
| T-04-14 | Information disclosure | Public and canary evidence | high | mitigate | Allowlisted evidence fields, operation correlation hash, hostname-only host evidence, structured redaction verifier, no media/URL/secret capture. |
| T-04-15 | Denial of service / Cost | Paid canary | high | mitigate | Blocking current budget authorization, one stable idempotency key, confirmation env guard, auto-reload off, kill switch, same-operation recovery. |
| T-04-16 | Repudiation | Cancellation/refund evidence | high | mitigate | Separate pre/post acceptance fixtures and ledger/entitlement audit keys; pending state until provider terminal truth. |
| T-04-SC | Tampering | Dependency supply chain | high | mitigate | No package installation; frozen lockfile plus full typecheck/build/security release gate. |
</threat_model>

<verification>
- Disposable PostgreSQL 17 migrations/schema/RLS check and concurrent settlement tests pass.
- Owner-only output refresh, expired URL, previous-success, pending cancellation, public redaction, and rendered browser evidence pass.
- All non-billable `eval:phase04`, test, typecheck, build, and redaction gates pass before paid authorization.
- One separately authorized Seedance 2.5 operation proves same-operation restart recovery, private valid/quality-approved output, exact settlement, and sanitized evidence.
- No unresolved high/critical ASVS threat remains.
</verification>

<success_criteria>
- Duplicate run/operation/charge/refund counts remain zero under concurrency and restart.
- Every accepted output is a MovPrompt-owned, fully validated private MP4 and the previous accepted result survives failure.
- Expired accepted-output access refreshes securely for the owner only.
- Provider/secret/URL/retry internals remain absent from public and evidence boundaries.
- Phase 04 remains incomplete until the current-budget canary checkpoint is approved with complete evidence.
</success_criteria>

<output>
Create `.planning/phases/04-durable-generation-and-accepted-quality/04-03-SUMMARY.md` and `04-CANARY-EVIDENCE.md` when done; do not mark the phase complete without the checkpoint evidence.
</output>
