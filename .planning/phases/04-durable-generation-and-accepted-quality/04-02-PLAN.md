---
phase: 04-durable-generation-and-accepted-quality
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/worker/src/output-quality-reviewer.ts
  - apps/worker/src/output-quality-reviewer.test.ts
  - apps/worker/src/benchmark-manifest.ts
  - apps/worker/src/benchmark-manifest.test.ts
  - apps/worker/src/__fixtures__/kw-video-48-v1.json
  - apps/worker/src/__fixtures__/kw-video-48-v1.calibration.json
  - apps/worker/src/main.ts
  - apps/worker/src/render-lifecycle.ts
  - apps/worker/src/render-lifecycle.test.ts
autonomous: false
requirements: [GEN-08, GEN-09, GEN-11, GEN-14]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
read_first:
  - .planning/phases/04-durable-generation-and-accepted-quality/04-01-SUMMARY.md
  - .planning/phases/04-durable-generation-and-accepted-quality/04-CONTEXT.md
  - .planning/phases/04-durable-generation-and-accepted-quality/04-AI-SPEC.md
  - .planning/phases/04-durable-generation-and-accepted-quality/04-VALIDATION.md
  - .planning/phases/04-durable-generation-and-accepted-quality/04-COVERAGE.md
  - packages/creative-engine/src/quality-engine.ts
  - packages/creative-engine/src/benchmark.ts
must_haves:
  truths:
    - Technical approval precedes a complete structured product/fact/Arabic/bilingual/safe-zone/visual/compliance decision (D-12, D-13).
    - Missing, invalid, duplicate, timed-out, unsupported, or ambiguous review evidence fails closed and never advances the accepted version (D-11, D-13).
    - Automation validates candidate checksums, evidence schema, reviewer independence, adjudication, metrics, thresholds, and version binding but never fabricates human reviewer labels or approval.
    - Automatic acceptance remains unavailable unless blocking human calibration records two independent qualified-reviewer labels per candidate, adjudication, reviewer-role attestations, an approval record, weighted kappa >= 0.70, every applicable per-dimension Spearman >= 0.70, and zero critical false accepts (D-12, D-13, D-24).
    - One quote allows one initial attempt plus at most two same-configuration internal quality retries and never adds a customer charge (D-14, D-15, D-16).
  artifacts:
    - path: apps/worker/src/output-quality-reviewer.ts
      provides: Deterministic complete-dimension acceptance and retry directive policy
    - path: apps/worker/src/__fixtures__/kw-video-48-v1.json
      provides: Facts-locked 48-candidate manifest with immutable candidate checksums and no fabricated reviewer labels
    - path: apps/worker/src/__fixtures__/kw-video-48-v1.calibration.json
      provides: Human-approved, versioned, sanitized independent-review labels, role attestations, adjudications, computed agreement metrics, thresholds, and approval record for the exact evaluator/rubric pair
    - path: apps/worker/src/render-lifecycle.ts
      provides: Maximum-two same-configuration internal quality retry path
  key_links:
    - from: apps/worker/src/benchmark-manifest.ts
      to: apps/worker/src/output-quality-reviewer.ts
      via: Runtime receives only a schema-valid, human-attested, approved calibration artifact bound to the exact candidate/evaluator/rubric checksums and versions
      pattern: reviewerRoleAttestations|approvalRecord|evaluatorVersion|rubricVersion|passed
    - from: apps/worker/src/output-quality-reviewer.ts
      to: apps/worker/src/render-lifecycle.ts
      via: Accepted/retry/rejected decision controls immutable attempt transition
      pattern: accepted|retry|rejected|prepareQualityRetry
    - from: apps/worker/src/__fixtures__/kw-video-48-v1.calibration.json
      to: apps/worker/src/main.ts
      via: Exact-version calibration eligibility is validated before reviewer composition and runtime readiness can advertise automatic acceptance
      pattern: evaluatorVersion|rubricVersion|passed|assertGenerationRuntimeReady
  prohibitions:
    - statement: Never charge or consume a new customer output for an internal quality retry; every retry remains inside the original quote and immutable configuration.
      status: unverified
      flagged: true
---

<objective>
**As a** Kuwait business owner, **I want to** receive only a playable video that preserves my exact product, facts, language, and layout, **so that** a polished but incorrect provider result never becomes my paid campaign.

Expand the technically accepted candidate path into human-calibrated, evidence-bearing visual/business review and bounded same-configuration quality retries per D-11 through D-16, D-20, D-21, and D-24.

Purpose: Make MovPrompt acceptance materially stronger than provider completion while retaining one accepted-output economic contract.
Output: Calibration schema/metrics validator, blocking qualified-human calibration approval, fail-closed runtime quality gate, and at-most-two internal retry lifecycle.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-CONTEXT.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-AI-SPEC.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-VALIDATION.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-COVERAGE.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-01-SUMMARY.md

<interfaces>
Keep the existing `RenderOutputPersister`, `OutputQualityAnalyzer`, composed `createOutputQualityReviewer`, creative-engine `evaluateAcceptedOutput`, immutable `render_attempts`, and `prepareQualityRetry` seams. AI observations never decide settlement, ownership, access, attempt count, or the accepted-version pointer. All verification uses local media/reference fixtures and mocked reviewer/provider calls.
</interfaces>

<execution_discipline>
Commit after each automated task using only that task's worker/fixture files and focused evidence. Do not mix frontend work into either worker commit, do not stage unrelated dirty-tree files, and do not create the human calibration artifact until blocking Task 04-02-03 supplies genuine reviewer evidence.
</execution_discipline>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 04-02-01: Build the calibration import, schema, metrics, and artifact validator</name>
  <read_first>apps/worker/src/benchmark-manifest.ts, apps/worker/src/benchmark-manifest.test.ts, packages/creative-engine/src/quality-engine.ts, packages/creative-engine/src/benchmark.ts, .planning/phases/04-durable-generation-and-accepted-quality/04-AI-SPEC.md Sections 5.2-5.4</read_first>
  <files>apps/worker/src/benchmark-manifest.ts, apps/worker/src/benchmark-manifest.test.ts, apps/worker/src/__fixtures__/kw-video-48-v1.json</files>
  <behavior>
    - The locked 48-candidate set covers all four verticals, three language modes, four challenge classes, and four ratios with immutable facts, candidate checksum, rights-attested private reference identity, evaluator output, and rubric version exactly once; it contains no manufactured reviewer label or approval.
    - The importer rejects evidence unless every candidate checksum matches, two distinct qualified-human reviewer roles have separately attested and completed labels, every case has a role-qualified adjudication, and clinic cases include the AI-SPEC clinic/compliance role attestation.
    - Deterministic computation produces binary confusion counts, weighted Cohen's kappa, per-dimension sample counts and tie-aware Spearman correlations, critical-defect false-accept count, exact evaluator/rubric/dataset versions, thresholds, reviewer-role attestation digests, and approval-record digest.
    - A calibration artifact validates only when all 48 cases are independently labeled and adjudicated, weighted kappa is at least 0.70, every applicable per-dimension Spearman is at least 0.70, critical false accepts equal zero, and an authorized calibration approver has signed the exact candidate/evaluator/rubric evidence digest.
    - Evidence with provider/signed/media URLs, provider operations, secrets, raw media, raw reviewer prose, personal reviewer names, missing role attestations, or absent approval is rejected (D-20, D-24).
  </behavior>
  <action>Write failing import/schema/metrics tests first, then extend the existing benchmark-manifest seam with strict Zod schemas and pure deterministic functions for candidate checksum validation, reviewer independence and role-attestation validation, adjudication completeness, confusion counts, weighted Cohen's kappa, tie-aware per-dimension Spearman correlation, threshold evaluation, approval-record verification, and exact evaluator/rubric/dataset version matching. Build `kw-video-48-v1.json` as a candidate-only manifest from the existing corpus and private fixture checksums. Provide an explicit import/validate path for qualified-human evidence, but do not generate, guess, seed, duplicate, or check in reviewer labels, adjudications, attestations, or approval records from automation. Tests may exercise rejected synthetic structures in test-local memory, but no synthetic reviewer evidence may become a production calibration artifact. The validator must recompute every metric and require deep equality before it can mark imported evidence valid.</action>
  <verify><automated>bun run --cwd apps/worker test --run src/benchmark-manifest.test.ts</automated></verify>
  <acceptance_criteria>
    - All 48 candidate IDs and candidate checksums occur exactly once and all 16 subject packs remain consistent across language variants.
    - The candidate manifest contains no reviewer labels, adjudication, role attestation, approval record, or provider/signed/media URL.
    - Imported evidence without two independently completed qualified-human labels, adjudication, role attestations, exact versions/checksums, or approval record fails validation.
    - Recomputed metrics must equal imported metrics and meet kappa >= 0.70, every applicable per-dimension Spearman >= 0.70, and zero critical false accepts before validation succeeds.
    - The paid canary cannot be imported as a calibration candidate or substitute for qualified-human evidence.
  </acceptance_criteria>
  <done>Automation can build candidates and validate/import genuine calibration evidence deterministically, but it cannot manufacture the human judgment needed for approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02-02: Enforce attested calibrated acceptance and bound quality retries</name>
  <reversibility rating="costly">Per D-14, retry accounting is coupled to quote economics, immutable attempt history, and exactly-once settlement across worker/database boundaries.</reversibility>
  <read_first>apps/worker/src/main.ts, apps/worker/src/output-quality-reviewer.ts, apps/worker/src/output-quality-reviewer.test.ts, apps/worker/src/render-lifecycle.ts, apps/worker/src/render-lifecycle.test.ts, apps/worker/src/benchmark-manifest.ts, apps/worker/src/__fixtures__/kw-video-48-v1.calibration.json, packages/db/src/generation-service.ts</read_first>
  <files>apps/worker/src/main.ts, apps/worker/src/output-quality-reviewer.ts, apps/worker/src/output-quality-reviewer.test.ts, apps/worker/src/render-lifecycle.ts, apps/worker/src/render-lifecycle.test.ts</files>
  <behavior>
    - `OutputQualityReviewer` can return `accepted` only when the loaded calibration artifact is schema-valid, human-attested, independently labeled, adjudicated, explicitly approved, passing, and exactly matches all candidate checksums plus the active evaluator/rubric versions; missing, stale, failed, or mismatched evidence returns fail-closed `needs_review`/non-accepted truth (D-12, D-13).
    - Worker quality readiness is unavailable when calibration eligibility fails, while in-flight candidate reconciliation remains safe; a runtime configuration/fingerprint cannot advertise automatic quality acceptance for an uncalibrated evaluator/rubric.
    - Technical plus every applicable product identity, prompt/fact adherence, motion/artifact, brand safety, dialect, speech sync, safe-zone, and compliance dimension appears exactly once; critical fact/Arabic/compliance defects reject regardless of aggregate score (D-12, D-13).
    - One quote permits attempt ordinals 0, 1, and 2 only: one initial attempt plus at most two internal quality retries (D-14).
    - A retry copies the same project/version/configuration hash, product/facts, copy, language, ratio, resolution, audio, references, and rights; only a deterministic failed-dimension directive changes (D-15).
    - Queue redelivery does not consume a quality retry; one rejected candidate creates at most one next attempt (D-14).
    - Exhaustion fails honestly and invokes the single terminal restoration path; it never accepts the highest rejected candidate (D-16).
    - A failed attempt cannot move the project's accepted-version pointer or remove the last accepted output (D-11, D-21).
  </behavior>
  <action>Write reviewer eligibility and retry-budget tests first. In `main.ts`, load calibration through the deterministic benchmark-manifest contract before composing the reviewer, bind it to the exact candidate checksums and active evaluator/rubric versions, require completed independent reviewer-role attestations plus adjudication and an authorized approval record, pass only that validated eligibility object into `createOutputQualityReviewer`, and make `assertGenerationRuntimeReady`/the runtime fingerprint fail closed when it is unavailable. The reviewer must return non-accepted `needs_review` when evidence is absent, malformed, unattested, unapproved, stale, failed, version/checksum-mismatched, below either 0.70 agreement threshold, or contains any critical false accept; already persisted operations remain recoverable. Preserve strict Zod observations, complete unique dimensions, deterministic hard gates, bounded evidence, and no AI authority over settlement. Separate provider/status reconciliation from quality attempt creation; validate the canonical immutable configuration hash before every retry and store only the bounded failed-dimension directive. Keep customer charge/reservation state outside retry creation so one accepted-output quote remains the sole economic unit. Do not make worker/frontend changes in the same task commit.</action>
  <verify><automated>bun run --cwd apps/worker test --run src/output-quality-reviewer.test.ts src/render-lifecycle.test.ts &amp;&amp; bun run eval:phase04</automated></verify>
  <acceptance_criteria>
    - Only a validator-passing human-attested and approved exact-version/checksum calibration plus complete quality observations can reach deterministic acceptance; missing/unattested/unapproved/failed/mismatched calibration cannot return accepted and makes automatic quality readiness unavailable.
    - Reviewer acceptance tests explicitly cover kappa below 0.70, one per-dimension Spearman below 0.70, one critical false accept, incomplete labels, stale evaluator version, and stale rubric version.
    - Critical product/fact/Arabic/compliance defect fixtures have zero false acceptance, and missing/duplicate/schema-invalid/timed-out evidence never approves.
    - Property/race fixtures never produce an attempt ordinal above 2 or more than one next attempt per rejected candidate.
    - All attempts share the same immutable configuration/facts hash and no retry mutates a customer-selected field.
    - Retry fixtures add no customer ledger mutation or entitlement consumption.
    - Exhausted/failing newer work leaves the previous accepted version/output untouched and reports a retryable project route.
    - `eval:phase04` requires the 48-case manifest, calibration recomputation, reviewer eligibility, retry, and media fixtures and skips no required Phase 04 check.
  </acceptance_criteria>
  <done>Only a currently human-attested, explicitly approved, validator-passing evaluator/rubric calibration can approve, while quality retries remain bounded internal attempts under one quote and preserve the user's last successful result.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 04-02-03: Complete and approve qualified-human Kuwait calibration</name>
  <what-built>The non-billable 48-candidate manifest, strict evidence importer/schema, deterministic metrics validator, fail-closed runtime acceptance gate, and bounded retry behavior. No automation-created reviewer labels or approval record exist.</what-built>
  <how-to-verify>
    1. Assign every candidate to two qualified human reviewers working independently. Each reviewer completes every required dimension and attests their qualifying role; clinic candidates include the clinic/compliance role required by AI-SPEC Section 1b. The reviewers must not see or copy one another's labels before both submissions are locked.
    2. Verify each locked label set references the exact candidate ID and checksum. A role-qualified adjudicator resolves every disagreement and attests the adjudication without overwriting either original label.
    3. Import the completed labels, reviewer-role attestations, adjudications, candidate checksums, dataset/evaluator/rubric versions, and the authorized calibration approval record through the implemented importer. Do not hand-edit computed metrics or mark `passed` manually.
    4. Run `bun run --cwd apps/worker test --run src/benchmark-manifest.test.ts &amp;&amp; bun run eval:phase04`. Confirm all 48 candidates are present exactly once, two independent labels and adjudication exist per candidate, role attestations and approval record validate, weighted kappa is >= 0.70, every applicable per-dimension Spearman is >= 0.70, and critical false accepts equal zero.
    5. Inspect `kw-video-48-v1.calibration.json`: it contains only sanitized reviewer-role identifiers/attestation digests, labels, adjudications, candidate checksums, versions, derived metrics, and approval digest—never names, prose, raw media, URLs, provider operations, or secrets.
    6. Confirm the paid Seedance canary has not been counted among the 48 candidates and cannot satisfy or waive this calibration gate. This checkpoint itself makes no provider call and needs no provider budget.
  </how-to-verify>
  <resume-signal>Reply `calibration approved` only after the two independent qualified-human review sets, adjudication, attestations, approval record, and validator results are complete; otherwise describe the missing or failed evidence. Phase 04 remains incomplete while this checkpoint is open.</resume-signal>
  <done>A genuine human-attested, independently reviewed, adjudicated, explicitly approved exact-version calibration artifact passes kappa/Spearman/critical-false-accept thresholds; automatic acceptance remains disabled until then.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Confirmed campaign/reference data to quality model | User/imported content is untrusted evidence, not evaluator instructions. |
| Quality-model result to deterministic policy | Model output is probabilistic and cannot directly accept, retry, settle, or mutate ownership. |
| Qualified-human calibration evidence to runtime | Reviewer labels, role attestations, adjudication, candidate checksums, versions, and approval must remain independent, authentic, complete, and immutable. |
| Retry directive to provider lifecycle | A directive may alter generation guidance but must preserve immutable customer facts/configuration. |

## STRIDE Threat Register — ASVS Level 1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-07 | Tampering / Elevation | Quality judge output | critical | mitigate | Strict schema, complete unique dimensions, deterministic hard gates, exact-version/checksum human-attested and approved calibration eligibility, fail-closed readiness, no direct settlement/pointer authority. |
| T-04-08 | Information disclosure | Reviewer prompt/evidence | high | mitigate | Confirmed bounded facts only, private capped media, no raw provider payload/URL/secret, bounded evidence and versioned evaluator. |
| T-04-09 | Tampering / Cost | Internal retry creation | critical | mitigate | Max two retries, immutable configuration hash comparison, one next attempt per rejected candidate, no per-retry customer settlement. |
| T-04-10 | Repudiation | Benchmark labels | high | mitigate | Candidate checksums, complete 48-ID manifest, two independently completed qualified-human labels, reviewer-role attestations, adjudication, approver record, reproducible metrics, and versioned evaluator/rubric evidence. |
| T-04-SC | Tampering | Dependency supply chain | high | mitigate | No package installation; use repository-pinned Zod, AI SDK, FFmpeg/FFprobe seams and frozen Bun lockfile. |
</threat_model>

<verification>
- The 48-candidate manifest, importer, schema, checksum, independence, attestation, adjudication, metric, threshold, and approval-record validators pass without fabricating reviewer labels.
- Blocking qualified-human calibration produces a sanitized approved artifact with two independent labels per candidate, kappa and every applicable Spearman >= 0.70, and no critical false accepts.
- Retry property tests prove three total attempts maximum, immutable facts/configuration, and no per-retry charge.
- `eval:phase04`, focused worker tests, typecheck, and build remain non-billable.
- No unresolved high/critical ASVS threat remains.
</verification>

<success_criteria>
- Only final private, technically valid output reviewed by an approved human-calibrated evaluator/rubric can be accepted.
- Exact product, fact, Arabic/bilingual, safe-zone, and compliance truth is enforced before project acceptance.
- Retry exhaustion fails and preserves the last accepted version instead of selecting a rejected candidate.
- No automated eval or fixture command can call a paid provider.
- Phase 04 cannot seal while the qualified-human calibration checkpoint is incomplete, regardless of code, tests, or paid-canary results.
</success_criteria>

<output>
Create `.planning/phases/04-durable-generation-and-accepted-quality/04-02-SUMMARY.md` when done.
</output>
