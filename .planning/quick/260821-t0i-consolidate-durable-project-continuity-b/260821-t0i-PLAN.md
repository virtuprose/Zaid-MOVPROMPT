---
quick_id: 260821-t0i
phase: quick-260821-t0i
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - .planning/STATE.md
  - .planning/PROGRESS.md
  - .planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-PLAN.md
  - .planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-VALIDATION.md
must_haves:
  truths:
    - "A fresh agent can identify the exact durable Git snapshot, current Phase 04 plan/task, proven checks, missing evidence, and next safe action without reading Codex chat history."
    - "Phase 04-01 is recorded as complete; Phase 04-02 automated Tasks 01 and 02 are recorded as implemented and non-billable-eval green, while blocking Task 04-02-03 remains open until genuine qualified-human calibration is independently completed, adjudicated, attested, approved, and validator-passing."
    - "No continuity document, automated test, agent, canary, or cleanup operation may invent reviewer labels, represent calibration as approved, waive Task 04-02-03, or mark Phase 04-02/Phase 04 complete prematurely."
    - "One paid Seedance 2.5 canary remains a separate later checkpoint: it requires approved exact-version human calibration first, then a new explicit current USD cap and one-operation authorization; no command in this quick task may make a paid provider call."
    - "Canonical product facts stay in AGENTS.md and .planning/PROJECT.md, milestone scope stays in .planning/ROADMAP.md, current execution state stays in .planning/STATE.md, and .planning/PROGRESS.md contains only the compact operational delta and links."
    - "Redundant MovPrompt sub-agent history pruning may begin only after the continuity packet passes a fresh-context test, its exact four-file commit is pushed, and a separate one-file readiness-marker commit records and publishes that proof; this plan never authorizes deletion of the repository, .git, the main MovPrompt rollout, active/open histories, or histories from another project."
    - "This continuity task must not configure or invoke CI/CD or GitHub Actions, must not modify any workflow file, and both documentation commits must include [skip ci] so push- and pull-request-triggered workflows are skipped."
  artifacts:
    - path: .planning/STATE.md
      provides: "Schema-compatible GSD current position, truthful blocker, and a single resume pointer."
    - path: .planning/PROGRESS.md
      provides: "Compact human handoff with Git baseline, completed/in-flight/open work, proof commands, canonical links, and explicit calibration/canary boundaries."
    - path: .planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-VALIDATION.md
      provides: "Recorded fresh-context comprehension result and the bounded go/no-go decision for later redundant-history pruning."
  key_links:
    - from: .planning/STATE.md
      to: .planning/PROGRESS.md
      via: "Session Continuity resume file points to the compact operational handoff."
      pattern: "Resume file: .planning/PROGRESS.md"
    - from: .planning/PROGRESS.md
      to: .planning/phases/04-durable-generation-and-accepted-quality/04-01-SUMMARY.md
      via: "Completed Phase 04-01 evidence is linked instead of copied."
      pattern: "04-01-SUMMARY.md"
    - from: .planning/PROGRESS.md
      to: .planning/phases/04-durable-generation-and-accepted-quality/04-02-PLAN.md
      via: "The exact open qualified-human calibration checkpoint remains canonical in Task 04-02-03."
      pattern: "04-02-03"
    - from: .planning/PROGRESS.md
      to: .planning/phases/04-durable-generation-and-accepted-quality/04-03-PLAN.md
      via: "The paid-canary dependency and authorization boundary remain canonical in Task 04-03-03."
      pattern: "04-03-03"
    - from: .planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-VALIDATION.md
      to: .planning/PROGRESS.md
      via: "A no-chat fresh agent must recover the handoff facts and prohibitions before pruning can be marked ready."
      pattern: ".planning/PROGRESS.md"
---

<objective>
Consolidate MovPrompt's current execution state into a small, repository-owned continuity packet before redundant Codex sub-agent histories are pruned.

Purpose: Preserve the decisions and operational state that a new agent actually needs while keeping Git, project files, Phase 04 evidence, and human/paid approval boundaries authoritative.
Output: An accurate `.planning/STATE.md`, one compact `.planning/PROGRESS.md`, and a fresh-context validation record that must pass before cleanup begins.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-01-PLAN.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-01-SUMMARY.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-02-PLAN.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-03-PLAN.md
@.planning/phases/04-durable-generation-and-accepted-quality/04-VALIDATION.md
@docs/IMPLEMENTATION_STATUS.md

Validated planning baseline on 2026-08-21:

- Branch `codex/production-rebuild` is synchronized with `origin/codex/production-rebuild` at `d2ce4116ad5d2ac2ae9fbac3deabf6018dbfee01` (`0` ahead, `0` behind before this plan file).
- Tracked work was clean; `.claude/`, `.codex/`, and `.gsd/` were pre-existing untracked tool/workflow directories and must remain untouched and unstaged.
- `04-01-SUMMARY.md` exists and records Phase 04-01 complete with zero paid provider calls.
- Phase 04-02 automated implementation is committed through `a8c859e`, `0d38c3c`, `dab7fd2`, `fa84038`, and `90f6f36`.
- `bun run eval:phase04` passed on the baseline: 9 provider tests; 39 worker tests passed with 2 intentionally skipped; 35 API tests; 2 database tests; no paid provider confirmation variable was used.
- `apps/worker/src/__fixtures__/kw-video-48-v1.calibration.json` and `04-02-SUMMARY.md` do not exist. Therefore Task 04-02-03 is open and Plan 04-02 is not complete.
- `docs/runbooks/PHASE4_PAID_CANARY.md` and `04-CANARY-EVIDENCE.md` do not exist. Therefore Plan 04-03 has not started and no paid canary is proven or authorized.
- `docs/IMPLEMENTATION_STATUS.md` is explicitly dated 2026-08-12 and remains historical demonstrated-evidence context, not the current Phase 04 resume authority.

Canonical ownership:

| File | Owns | Must not become |
|---|---|---|
| `AGENTS.md` | Product invariants, stack, architecture, and working rules | A session diary |
| `.planning/PROJECT.md` | Product definition, constraints, and locked decisions | A commit/status log |
| `.planning/ROADMAP.md` | Phase goals, dependencies, and exit criteria | A task-by-task handoff |
| `.planning/STATE.md` | Machine-readable current position and resume pointer | A long narrative |
| `.planning/PROGRESS.md` | Short current delta, proof, blockers, and next safe action | A duplicate product specification |
| Phase plans/summaries/validation | Detailed implementation contract and evidence | A generic project overview |
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reconcile the canonical current state and write one compact operational handoff</name>
  <files>.planning/STATE.md, .planning/PROGRESS.md</files>
  <action>Re-run the read-only Git and artifact checks from this plan before editing. Update `.planning/STATE.md` without changing its schema: Phase 4 remains executing; 04-01 is the one completed Phase 04 plan; the current plan is 04-02; automated Tasks 04-02-01 and 04-02-02 are implemented; execution is stopped at blocking Task 04-02-03; the completed-plan count includes 04-01; and Session Continuity points only to `.planning/PROGRESS.md`. Do not describe 04-02 or Phase 04 as complete.

Create `.planning/PROGRESS.md` as a compact operational delta, not a second PROJECT/ROADMAP. Include: the current branch/upstream and baseline code snapshot; an exact completed/in-flight/open table; the latest non-billable `eval:phase04` result; the absent human calibration and paid-canary artifacts; the safe resume read order; the next permitted engineering/human action; a short `Not proven` section; and links to the canonical Phase 04 plans/summary/validation. State plainly that automation cannot create reviewer evidence and that the paid canary is both downstream of approved calibration and separately budget-authorized. Record `.claude/`, `.codex/`, and `.gsd/` only as pre-existing local tool directories that are not part of the continuity commit; do not inspect, edit, stage, or summarize their contents. Do not edit `AGENTS.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, or `docs/IMPLEMENTATION_STATUS.md`, because those already own their respective facts. Never copy secrets, signed URLs, provider operation IDs, customer media, raw reviewer prose, or chat transcripts into either continuity file.</action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD &amp;&amp; git rev-parse @{upstream} &amp;&amp; git rev-list --left-right --count HEAD...@{upstream} &amp;&amp; env -u MOVPROMPT_GATEWAY_CONFIRM_PAID_VIDEO bun run eval:phase04 &amp;&amp; test ! -e apps/worker/src/__fixtures__/kw-video-48-v1.calibration.json &amp;&amp; test ! -e .planning/phases/04-durable-generation-and-accepted-quality/04-02-SUMMARY.md &amp;&amp; test ! -e docs/runbooks/PHASE4_PAID_CANARY.md &amp;&amp; test ! -e .planning/phases/04-durable-generation-and-accepted-quality/04-CANARY-EVIDENCE.md &amp;&amp; rg -n "04-02-03|qualified-human|explicit current USD|04-03-03|Not proven|Resume file: .planning/PROGRESS.md" .planning/STATE.md .planning/PROGRESS.md &amp;&amp; git diff --check -- .planning/STATE.md .planning/PROGRESS.md</automated>
  </verify>
  <done>`STATE.md` and `PROGRESS.md` agree on the exact Git-backed Phase 04 position, link rather than duplicate canonical facts, make the human and paid gates impossible to misread, and identify one safe resume path without chat history.</done>
</task>

<task type="auto">
  <name>Task 2: Prove a no-chat fresh agent can resume safely, then publish the continuity packet</name>
  <files>.planning/STATE.md, .planning/PROGRESS.md, .planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-PLAN.md, .planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-VALIDATION.md</files>
  <action>Start a genuinely fresh read-only agent with no inherited conversation. Give it only the repository path and instruct it to read, in order, `AGENTS.md`, `.planning/STATE.md`, `.planning/PROGRESS.md`, the Phase 04 section of `.planning/ROADMAP.md`, `04-01-SUMMARY.md`, `04-02-PLAN.md`, `04-03-PLAN.md`, and `04-VALIDATION.md`. Ask it to report: durable branch/snapshot; exact current phase/plan/task; what automated work is proven; which artifacts/evidence are absent; whether it may fabricate or self-approve calibration; whether it may make a paid call; the next safe non-billable command/action; and which project/runtime claims remain unproven. It must answer that Task 04-02-03 is blocking, genuine qualified-human evidence cannot be fabricated, and no canary may run before approved exact-version calibration plus a separate current budget authorization.

Record the prompt scope, the agent's concise answers, pass/fail per question, any mismatch and correction, verification date, reviewed baseline, and a literal link to `.planning/PROGRESS.md` in `260821-t0i-VALIDATION.md`; do not paste a full chat/tool transcript. If any answer is wrong or overclaims readiness, correct only the listed `STATE.md`, `PROGRESS.md`, `PLAN.md`, or `VALIDATION.md` artifacts as needed and repeat with a new fresh context until all questions pass.

Use this non-circular publication sequence:

1. Set validation status to `PASS — PUBLICATION PENDING`; do not write the `READY FOR REDUNDANT MOVPROMPT SUB-AGENT HISTORY PRUNING` marker yet.
2. Stage exactly these four paths and no others: `.planning/STATE.md`, `.planning/PROGRESS.md`, this `260821-t0i-PLAN.md`, and `260821-t0i-VALIDATION.md`. Prove the staged name list contains exactly those four paths. Inspect both `git diff --cached --name-status` and the complete `git diff --cached --no-ext-diff`; record `Continuity staged paths: PASS` and `Continuity prohibited-material review: PASS` in the validation file only after confirming there are no secrets, signed/provider URLs, raw provider operation IDs, customer media, raw reviewer prose, reviewer identities, or unrelated paths. Restage the validation file and repeat the exact allowlist and full-diff review after recording the attestations.
3. Do not configure or invoke CI/CD or GitHub Actions and do not modify `.github/workflows/` or any other workflow file. Commit those four paths as `docs(continuity): preserve MovPrompt handoff [skip ci]`, push `codex/production-rebuild`, record the resulting continuity commit SHA, and prove with `git diff-tree` that the published commit contains exactly the four allowlisted paths. Verify upstream is `0` ahead and `0` behind. At this point continuity is durable, but pruning is still not ready because the marker is intentionally absent.
4. Update only `260821-t0i-VALIDATION.md` with the published continuity commit SHA, upstream verification, exact committed-path proof, and the final `READY FOR REDUNDANT MOVPROMPT SUB-AGENT HISTORY PRUNING` marker. Record `Readiness-marker staged paths: PASS` and `Readiness-marker prohibited-material review: PASS` after reviewing the complete one-file staged diff. Stage only that validation file, commit as `docs(continuity): mark MovPrompt history pruning ready [skip ci]`, push, and prove with `git diff-tree` that this second commit changed exactly the validation file and upstream is again `0` ahead and `0` behind.

The final marker permits only the separately audited redundant MovPrompt sub-agent rollout set; it does not permit deleting the main MovPrompt rollout, any active/open file, any other project's history, the repository, `.git`, or local working files.</action>
  <verify>
    <automated>validation_file=.planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-VALIDATION.md &amp;&amp; continuity_commit=$(git log -1 --format=%H --grep='^docs(continuity): preserve MovPrompt handoff \[skip ci\]$') &amp;&amp; readiness_commit=$(git log -1 --format=%H --grep='^docs(continuity): mark MovPrompt history pruning ready \[skip ci\]$') &amp;&amp; test -n "$continuity_commit" &amp;&amp; test -n "$readiness_commit" &amp;&amp; git diff-tree --no-commit-id --name-only -r "$continuity_commit" | awk '$0 == ".planning/STATE.md" {state++} $0 == ".planning/PROGRESS.md" {progress++} $0 == ".planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-PLAN.md" {plan++} $0 == ".planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-VALIDATION.md" {validation++} {count++} END {exit !(count == 4 &amp;&amp; state == 1 &amp;&amp; progress == 1 &amp;&amp; plan == 1 &amp;&amp; validation == 1)}' &amp;&amp; git diff-tree --no-commit-id --name-only -r "$readiness_commit" | awk '$0 == ".planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-VALIDATION.md" {validation++} {count++} END {exit !(count == 1 &amp;&amp; validation == 1)}' &amp;&amp; rg -n "PASS|04-02-03|cannot.*fabricat|separate.*budget|Not proven|\.planning/PROGRESS\.md|Continuity staged paths: PASS|Continuity prohibited-material review: PASS|Readiness-marker staged paths: PASS|Readiness-marker prohibited-material review: PASS|READY FOR REDUNDANT MOVPROMPT SUB-AGENT HISTORY PRUNING" "$validation_file" .planning/PROGRESS.md &amp;&amp; rg -n "$continuity_commit" "$validation_file" &amp;&amp; test "$(git rev-parse --abbrev-ref HEAD)" = "codex/production-rebuild" &amp;&amp; git rev-list --left-right --count HEAD...@{upstream} | awk '$1 == 0 &amp;&amp; $2 == 0 {ok=1} END {exit !ok}'</automated>
    <human-check>Read the recorded fresh-agent answers and both reviewed-diff attestations as if no chat history existed. Confirm they name 04-02-03 as the current blocker, distinguish automated validation from genuine reviewer approval, forbid an unauthorized paid call, identify the exact next safe action, prove the first commit contains only the four continuity artifacts, prove the readiness commit contains only the validation file, and state that the complete staged diffs contained no prohibited sensitive material.</human-check>
  </verify>
  <done>A fresh agent passes the complete continuity test; the exact four-file continuity commit is durable on GitHub; a later one-file marker commit records that publication proof and reviewed-diff attestations; and only then are the separately audited redundant MovPrompt sub-agent histories eligible for pruning.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Stale `STATE.md` causes a new agent to restart 04-01 or skip the 04-02 human gate | high | Reconcile against Git/artifact evidence and require fresh-context answers. |
| Automation manufactures reviewer labels or treats validator code as calibration approval | critical | Explicit absent-artifact checks, exact Task 04-02-03 link, and a blocking no-fabrication answer. |
| A previous discussion or account balance is treated as paid-call authorization | high | Require approved exact-version calibration first and a separate current USD cap/one-operation authorization. |
| Continuity files leak secrets, signed URLs, provider operations, customer media, or reviewer identities | high | Store bounded status and links only; inspect the exact diff before commit. |
| Cleanup removes active/main/other-project history or repository data | critical | The validation marker is scoped to an independently audited redundant MovPrompt sub-agent target list only. |
| Documentation work stages pre-existing local tool directories or unrelated user work | high | Stage only the four named planning artifacts and verify the exact diff. |
</threat_model>

<verification>
- [ ] Git branch/upstream and baseline code snapshot are recorded from live commands, not memory.
- [ ] `bun run eval:phase04` is green without the paid-provider confirmation guard.
- [ ] The human calibration artifact and 04-02 summary remain absent and are described as blocking, not forgotten.
- [ ] The paid-canary runbook/evidence remain absent and no canary authorization is inferred.
- [ ] A no-chat fresh agent answers every resume and safety question correctly.
- [ ] The first published commit contains exactly STATE, PROGRESS, PLAN, and VALIDATION; its complete staged diff is reviewed and recorded free of prohibited sensitive material.
- [ ] A separate later commit changes only VALIDATION to add the published continuity SHA and readiness marker; its complete staged diff is reviewed and pushed; upstream is `0` ahead and `0` behind.
- [ ] No cleanup occurs inside this plan.
</verification>

<success_criteria>
- MovPrompt's code and decisions remain recoverable from Git and repository-owned planning files without relying on the large Codex sub-agent histories.
- A fresh agent resumes at blocking Task 04-02-03 and does not repeat completed Phase 04-01/04-02 automated work.
- Human calibration and the paid Seedance canary remain distinct, explicit, unwaived gates.
- The later cleanup operation has a narrow go/no-go marker but receives no authority over the main rollout, active histories, other projects, `.git`, or working files.
</success_criteria>

<output>
After execution, create `.planning/quick/260821-t0i-consolidate-durable-project-continuity-b/260821-t0i-SUMMARY.md`. Do not mark the quick task complete until the continuity commit is pushed and the fresh-context validation passes.
</output>
