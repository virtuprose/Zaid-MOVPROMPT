# Campaign fact labels, output text and availability

Continue locally under the previously approved GSD bypass; no paid provider calls, push or deployment.

1. Write regressions for required/optional labels, preserved setup errors and optional video facts.
2. Remove empty-state chatter; mark required inputs with a red asterisk and accessible required semantics.
3. Carry booking links through the creative brief and prompt. Render entered offer/contact facts as deterministic video layers before private R2 master/preview persistence; omit empty values. Verify actual media locally without a new provider call.
4. Diagnose the actual campaign quote failure, expose actionable configuration errors and safe retry rather than a generic outage message, and confirm local API/worker readiness.
5. Run affected tests/types/build and browser checks; document limitations and keep existing saved outputs unchanged.
