---
quick_id: 260820-1nr
phase: quick-260820-1nr
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - AUTH-01
  - AUTH-06
  - AUTH-07
  - AUTH-09
  - SOURCE-05
  - SOURCE-06
files_modified:
  - apps/web/src/features/create/CreateStudio.tsx
  - apps/web/src/features/create/creatorAssets.ts
  - apps/web/src/features/create/creatorAssets.test.ts
  - apps/web/src/features/create/GuestClaimProgress.tsx
  - apps/web/src/features/create/GuestClaimProgress.test.tsx
  - apps/web/src/features/create/CreatorProgress.tsx
  - apps/web/src/features/create/CreatorProgress.test.tsx
  - apps/web/src/features/create/creator.css
  - .planning/quick/260820-1nr-fix-phase-2-ui-blockers-truthful-private/260820-1nr-BROWSER-EVIDENCE.md
must_haves:
  truths:
    - "After an authenticated user selects Generate, a dedicated private-claim surface shows the real creating, per-image, and verification stages before render submission."
    - "Cancel and keep editing aborts only the current browser claim attempt, returns to the unchanged campaign review, restores focus to Generate video, and retains the pending intent and every local image for an idempotent retry."
    - "A failed image claim identifies the failed local image internally, presents the existing Retry securing image and Replace image recovery, and never converts an aborted claim into an error."
    - "Retry price has a visible 3px amber focus ring in light and dark themes using the existing creator action token."
    - "At 375px, Add a link or photos is encountered before the empty preview and the progress indicator states the current step in readable English or Arabic text."
    - statement: "The corrected Create flow is rendered without overflow at 375, 768, 1024, and 1440 pixels in English/Arabic and light/dark, with keyboard-visible Retry price focus and a cancellable real claim state."
      verification: backstop
  artifacts:
    - path: "apps/web/src/features/create/GuestClaimProgress.tsx"
      provides: "The dedicated semantic claim-status surface and cancel action."
    - path: "apps/web/src/features/create/creatorAssets.ts"
      provides: "Abortable claim orchestration with factual stage callbacks."
    - path: "apps/web/src/features/create/CreatorProgress.tsx"
      provides: "Accessible desktop progress and readable mobile current-step summary."
    - path: "apps/web/src/features/create/creator.css"
      provides: "Semantic claim styling, responsive source-first ordering, and token-correct focus treatment."
    - path: ".planning/quick/260820-1nr-fix-phase-2-ui-blockers-truthful-private/260820-1nr-BROWSER-EVIDENCE.md"
      provides: "Observed responsive, theme, RTL, keyboard, and claim-recovery evidence with unverified states labelled honestly."
  key_links:
    - from: "apps/web/src/features/create/CreateStudio.tsx"
      to: "apps/web/src/features/create/creatorAssets.ts"
      via: "claimGuestAssets receives one AbortSignal and one onProgress callback for the active pendingGenerationId."
    - from: "apps/web/src/features/create/CreateStudio.tsx"
      to: "apps/web/src/features/create/GuestClaimProgress.tsx"
      via: "The real claim callback state selects the stage copy and renders the single polite status region."
    - from: "apps/web/src/features/create/CreatorProgress.tsx"
      to: "apps/web/src/features/create/creator.css"
      via: "The mobile current-step summary is visible while the desktop step labels retain their established hierarchy."
---

<objective>
Fix the remaining Phase 2 creator blockers so a novice Kuwait business owner sees truthful private-campaign progress, can cancel without losing work, can recover pricing by keyboard, and reaches the source task first on mobile.

Purpose: Restore visibility, control, and trust at the exact point where local campaign media becomes a private authenticated project, without redesigning the creator or contacting a paid generation provider.
Output: An abortable progress-aware private claim path, a focused claim-status UI, corrected responsive progress/source hierarchy, keyboard-visible price recovery, regression tests, and rendered-browser evidence.
</objective>

<execution_context>
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/workflows/execute-plan.md
@/Users/muhammadzaid/Desktop/Projects/moveprompts/.codex/gsd-core/templates/summary.md
@/Users/muhammadzaid/.codex/skills/ux-research-ia/SKILL.md
@/Users/muhammadzaid/.codex/skills/visual-art-direction/SKILL.md
@/Users/muhammadzaid/.codex/skills/design-system-token-architect/SKILL.md
@/Users/muhammadzaid/.codex/skills/interaction-state-microcopy/SKILL.md
@/Users/muhammadzaid/.codex/skills/accessibility-visual-qa/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-CONTEXT.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-UI-REVIEW.md
@.planning/phases/02-guest-authentication-and-data-integrity/02-BROWSER-EVIDENCE.md
@apps/web/src/features/create/CreateStudio.tsx
@apps/web/src/features/create/creatorAssets.ts
@apps/web/src/features/create/creatorAssets.test.ts
@apps/web/src/features/create/creator.css
@apps/web/src/i18n/translations/en.ts
@apps/web/src/i18n/translations/ar.ts

UX brief: The primary user has no knowledge of authentication, private storage, or video tooling. Their top tasks are to understand what MovPrompt is doing, stop safely if anxious, and continue without repeating work. The screen's single job during claim is to explain the current factual stage and offer Cancel and keep editing. Success means the next action and data-safety outcome are understandable within five seconds.

Visual thesis: Preserve the calm editorial creator workspace, warm-white/near-black surfaces, generous whitespace, and amber action accent. The claim state is one centered, quiet status panel rather than a dashboard, card grid, fake video preview, percentage, provider status, or estimated time. Reuse the established semantic creator tokens, typography roles, 44px controls, restrained motion, RTL system, and reduced-motion behavior.

Interaction contract: Default claim state is creating; stage changes are polite and factual; success moves to verification; cancellation returns to review without cleanup; an affected-image failure exposes one retry path and one replacement path; offline/error retains all inputs; focus returns to the most useful recovery action.
</context>

<source_audit>
SOURCE | ITEM | TASK | STATUS | NOTES
GOAL | Truthful private-claim progress and cancel recovery | 1 | COVERED | Real claim callbacks drive one semantic state surface and cancellation retains the draft.
GOAL | Retry price focus | 2 | COVERED | Existing action token supplies the required focus ring.
GOAL | Mobile source-first order and current-step label | 2 | COVERED | DOM/CSS order and progress markup are corrected together.
REQ | AUTH-01, AUTH-06, AUTH-07, AUTH-09 | 1, 2, 3 | COVERED | The complete guest campaign remains unchanged and recoverable through claim cancel/failure.
REQ | SOURCE-05, SOURCE-06 | 1, 3 | COVERED | Private asset progress and recovery correspond to the real owner-scoped upload path.
RESEARCH | None supplied for this quick task | — | EXCLUDED | Existing React/Vitest/CSS patterns are sufficient; no dependency choice or package install is needed.
CONTEXT | Guests configure before auth; local blobs clear only after verified claim | 1 | COVERED | Abort and failure preserve IndexedDB; existing verified cleanup remains authoritative.
CONTEXT | Reuse creator composition, semantic tokens, RTL, and accessible states | 1, 2, 3 | COVERED | No new visual system or flow is introduced.
CONTEXT | Cross-device drafts, billing, Digital Twins, and other deferred items | — | EXCLUDED | Explicitly outside this UI-blocker repair.
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire real private-claim stages and safe browser cancellation end to end</name>
  <files>apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creatorAssets.ts, apps/web/src/features/create/creatorAssets.test.ts, apps/web/src/features/create/GuestClaimProgress.tsx, apps/web/src/features/create/creator.css</files>
  <read_first>apps/web/src/features/create/guestClaimRecovery.ts, apps/web/src/features/create/guestDraftStore.ts, apps/web/src/i18n/translations/en.ts, apps/web/src/i18n/translations/ar.ts, .planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: a two-image claim reports creating, image 1 of 2, image 2 of 2, and checking saved details in the same order as the server checkpoints.
    - Test 2: the same AbortSignal reaches start, reservation, content upload, completion, checkpoint refresh, finalization, and owned-preview refresh requests made after it is supplied.
    - Test 3: aborting rejects as an abort, does not finalize the claim, and leaves the caller-owned blob map unchanged for retry.
    - Test 4: a non-abort asset failure still exposes the failed localAssetId so CreateStudio can focus the exact existing recovery path.
  </behavior>
  <action>Start by adding the failing cases above to creatorAssets.test.ts. In creatorAssets.ts, export a discriminated GuestClaimProgress type for creating, asset with current/total, and verifying stages; extend claimGuestAssets with optional signal and onProgress inputs; report stages only at real lifecycle boundaries; pass the signal through every fetch in the guest-claim path and assert it before the idempotent reservation retry. Preserve the existing canonical receipt, checksum, owner namespace, source-persistence, and exactly-once cleanup behavior.

Create GuestClaimProgress.tsx as a focused presentational surface that receives the current factual stage, the existing localized copy, and onCancel. Use one role=status aria-live=polite aria-atomic=true region for the heading/detail/stage, put the 44px Cancel and keep editing button outside that live region, and include no progressbar, percentage, video image, provider/model language, or time estimate. Style it in creator.css with existing creator canvas/panel/soft/line/ink/muted/action tokens, logical properties, the approved 25px heading and 12/14/16px text roles, one restrained 160ms entrance, and a reduced-motion fallback.

In CreateStudio.tsx, keep one AbortController ref per active claim, translate the pre-existing creator.claim keys through useLanguage().t, and render GuestClaimProgress only after the authenticated claim actually begins. Feed claimGuestAssets progress into this state. Cancel must abort the client attempt, clear busy/claim-view state, return to the unchanged campaign review, retain pendingGenerationId, rights, product images, facts, and IndexedDB data, announce the existing Nothing changed copy once, and restore focus to Generate video after it remounts. Treat AbortError as cancellation rather than claim failure. For GuestClaimAssetFailure, pass localAssetId into selectGuestClaimRecovery so Retry securing image resumes the exact failed asset. Clear the controller in finally only when it is still the active attempt. Do not call generation, provider, billing, or server-side destructive cancellation from this UI action.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/creatorAssets.test.ts &amp;&amp; bun run typecheck:web</automated>
  </verify>
  <done>The real authenticated claim path drives the dedicated status surface; its stage callbacks and abort propagation are tested; cancel returns to an unchanged, focused review; abort never shows a false failure; per-image failure retains exact retry context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct price focus, mobile task order, and readable step identity</name>
  <files>apps/web/src/features/create/GuestClaimProgress.test.tsx, apps/web/src/features/create/CreatorProgress.tsx, apps/web/src/features/create/CreatorProgress.test.tsx, apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creator.css</files>
  <read_first>apps/web/src/features/create/CreateStudio.tsx, apps/web/src/features/create/creator.css, .planning/phases/02-guest-authentication-and-data-integrity/02-UI-REVIEW.md, .planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: GuestClaimProgress exposes one polite status region, a named cancel button, the exact current stage, and no progressbar or invented percentage/time/provider copy.
    - Test 2: CreatorProgress exposes aria-valuenow/aria-valuetext and aria-current for the active step while rendering Step 1 of 5: Source in English.
    - Test 3: the same component renders the complete Arabic current-step summary with the current Arabic label and preserves semantic progress values.
  </behavior>
  <action>Write the component tests first. Extract the existing Progress markup from CreateStudio.tsx into CreatorProgress.tsx without changing the desktop sequence or accessibility contract. Add a mobile-only current-step summary with complete language-specific text, wrap desktop step text in a named element, and retain the numbered/checkmarked circles as supporting context. At max-width 800px, show the readable current-step summary and hide only the repeated per-circle text; remove the font-size:0 technique.

Keep the existing source section before the preview in JSX and remove the mobile rule that moves creator-product-card ahead of it, so Campaign source, source tabs, and Add a link or photos remain the first workspace task at 375px. Do not hide or reorder the primary source CTA. Change creator-cost-retry:focus-visible to a 3px outline using --creator-action with the existing 3px offset; do not introduce a new color or use the undefined creator-accent variable. Ensure claim, retry, and progress selectors rely on semantic light/dark tokens and logical inline positioning. Preserve 44px targets and reduced-motion behavior.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/GuestClaimProgress.test.tsx src/features/create/CreatorProgress.test.tsx src/features/create/creatorAssets.test.ts &amp;&amp; bun run typecheck:web &amp;&amp; bun run build:web</automated>
  </verify>
  <done>The claim surface has semantic component coverage, Retry price has a token-correct 3px focus ring, the mobile source task precedes the preview, and both languages expose a readable current-step summary without weakening desktop progress semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the corrected flow in the rendered browser and record honest evidence</name>
  <files>.planning/quick/260820-1nr-fix-phase-2-ui-blockers-truthful-private/260820-1nr-BROWSER-EVIDENCE.md</files>
  <read_first>/Users/muhammadzaid/.codex/skills/accessibility-visual-qa/SKILL.md, .planning/phases/02-guest-authentication-and-data-integrity/02-BROWSER-EVIDENCE.md, .planning/phases/02-guest-authentication-and-data-integrity/02-UI-SPEC.md</read_first>
  <action>Run the existing local web/API/PostgreSQL/MinIO stack and use the available browser-control tooling; do not install a browser package. Exercise the source-first Create screen, retryable quote failure, and authenticated local private-asset claim without reaching render submission or any paid provider. For the claim path, use local assets and stop with Cancel and keep editing while the factual stage is visible; confirm the same product, images, campaign facts, pending intent, and Generate focus remain, then retry and confirm the server checkpoint is resumed rather than duplicated.

Inspect 375, 768, 1024, and 1440 widths in English/LTR and Arabic/RTL, light and dark. At 375px confirm the source heading/tabs/action appear before the empty preview and the current-step summary is readable. Keyboard-tab to Retry price and observe the 3px amber ring in both themes. Inspect the claim panel for one live status region, no percentage/media/provider/time copy, 44px cancel target, correct wrapping, logical RTL alignment, no horizontal overflow, no clipped controls, and reduced-motion behavior. Record exact route, viewport, language, theme, observed result, commands, console state, and screenshot location in 260820-1nr-BROWSER-EVIDENCE.md. Label any runtime path that cannot be exercised as NOT VERIFIED and leave the relevant must-have open; never infer browser success from source or unit tests.</action>
  <verify>
    <automated>bun run --cwd apps/web test -- src/features/create/GuestClaimProgress.test.tsx src/features/create/CreatorProgress.test.tsx src/features/create/creatorAssets.test.ts &amp;&amp; bun run lint -- apps/web/src/features/create/CreateStudio.tsx apps/web/src/features/create/creatorAssets.ts apps/web/src/features/create/GuestClaimProgress.tsx apps/web/src/features/create/CreatorProgress.tsx &amp;&amp; bun run typecheck:web &amp;&amp; bun run build:web</automated>
    <human-check>Review the recorded screenshot matrix and explicit PASS/NOT VERIFIED labels; the quick task is not complete while any blocker path is merely inferred.</human-check>
  </verify>
  <done>Automated checks pass and the evidence file records observed responsive, RTL, theme, keyboard, cancellation, recovery, and console results without paid generation or an unsupported readiness claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser draft to authenticated claim API | Local campaign facts and blobs cross into an owner-scoped project only after authentication. |
| Browser cancellation to durable server checkpoints | Cancellation stops client work while completed server checkpoints remain idempotently resumable. |
| Claim diagnostics to user-facing UI | Internal object keys, signed URLs, account identifiers, and provider details must not cross into status or error copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-260820-01 | Tampering | CreateStudio cancel/retry state | high | mitigate | Preserve pendingGenerationId, immutable snapshot/checkpoint, local blobs, and canonical cleanup gate; test retry after abort. |
| T-260820-02 | Information Disclosure | GuestClaimProgress and recovery errors | high | mitigate | Render only localized business-language stages and affected-image recovery; never display object keys, signed URLs, other-account data, or raw server payloads. |
| T-260820-03 | Denial of Service | Abortable multi-request claim | medium | mitigate | Propagate one AbortSignal through the active attempt, stop new requests after abort, and resume through existing idempotent server checkpoints. |
| T-260820-04 | Repudiation | Browser evidence | low | mitigate | Record exact observed routes/viewports/states and mark unavailable runtime paths NOT VERIFIED. |
| T-260820-SC | Tampering | Package supply chain | low | accept | No dependency or package installation is permitted by this plan. |
</threat_model>

<verification>
Before declaring the quick task complete:

- [ ] creatorAssets tests prove ordered factual stages, full signal propagation, abort-before-finalize, retained blobs, and failed-asset identity.
- [ ] GuestClaimProgress and CreatorProgress component tests pass in English and Arabic.
- [ ] Web lint, typecheck, and production build pass without changing unrelated dirty-tree files.
- [ ] Browser evidence covers 375, 768, 1024, and 1440 in both languages and themes, or keeps the task open with explicit NOT VERIFIED labels.
- [ ] No paid generation/provider call, package install, visual redesign, draft deletion on cancel, fake percentage, or generation-state reuse was introduced.
</verification>

<success_criteria>

- The real private claim is visible and cancellable before generation submission.
- Cancellation and failure retain the exact campaign and local media and provide an obvious next action.
- Retry price has reliable keyboard focus in both themes.
- The mobile first viewport prioritizes the source task and names the current step.
- Responsive, RTL, theme, keyboard, and recovery evidence is explicit and honest.
- Unrelated worktree changes remain untouched and unstaged.
</success_criteria>

<output>
After completion, create `.planning/quick/260820-1nr-fix-phase-2-ui-blockers-truthful-private/260820-1nr-SUMMARY.md`.
</output>
