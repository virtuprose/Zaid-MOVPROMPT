1. Copy `user-uploads://n888.mp4` to `public/presets/cinematic-fashion.mp4`.
2. In `src/lib/marketingStudio.ts`, on the Cinematic Fashion preset, add `video: "/presets/cinematic-fashion.mp4"` so the picker plays the video (image stays as poster fallback).