

## Change Admin Dashboard Route

Update the route from `/admin/analytics` to `/admin` in `src/App.tsx`. The dashboard will then be accessible at `movprompt.com/admin`.

### Change
**File: `src/App.tsx`**
- Change `path="/admin/analytics"` to `path="/admin"`

One-line change.

