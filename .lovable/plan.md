## Change
Remove the "This usually takes ~30s" caption in `src/pages/MarketingStudio.tsx` (line 1085) since real Seedance 2.0 renders take 1–3 minutes.

Replace with: `Rendering — usually 1–3 minutes`.

## Files
- `src/pages/MarketingStudio.tsx` — single-line copy edit.
